# STATE.md — Estado vivo del proyecto Trazalog v3

> **Este archivo vive en `traz-tools/doc/v3/STATE.md`.** Es el ÚNICO documento de estado del proyecto — reemplaza kickoffs-como-estado, backlog xlsx, y resúmenes pegados al chat.
>
> **Por qué solo en traz-tools y no duplicado en traz-comp-dnato:** el estado de un proyecto con dos repos necesita una sola fuente para no repetir el problema de desincronización que motivó esta metodología (dos copias = deriva garantizada). Se eligió `traz-tools` como master porque concentra más volumen de trabajo (WSO2/APIs/MCP) y es donde vive el Project board de GitHub. `traz-comp-dnato/CLAUDE.md` tiene una línea que apunta acá (ver bloque E4).
>
> **Reglas de actualización:**
> - Claude Code actualiza este archivo al final de CADA tarea (🟢🟡🔴), como parte de su Definition of Done. No es opcional.
> - Claude Web lo lee al inicio de cada sesión, antes de asumir cualquier estado.
> - Rodolfo + Claude Web lo revisan juntos en el ritual semanal (ver CICD_STRATEGY §5-bis.7) para corregir cualquier deriva.
> - Formato: mantener SIEMPRE la estructura de secciones. No agregar prosa larga — es un tablero, no una bitácora.

---

**Sprint actual:** Sprint 3 — Activación early adopter minero
**Objetivo del sprint:** Reemplazar ngrok por un despliegue estable en GCP (ADR-011) y cerrar la deuda técnica pendiente antes de activar al primer cliente early adopter.
**Última actualización:** 2026-08-08 por Claude Code (reconciliación: E7-INFRA-03 quedó huérfano tras squash-merge, ver Decisiones recientes)

### Tareas activas

| ID | Descripción | Clase | Estado | Rama / PR |
|---|---|---|---|---|
| DOC-PRD-SPEC | Spec funcional y técnica del módulo de Producción (PRD / `traz-prod-trazasoft`, esquema `prd`) — pedido ad-hoc del PM como base para corrección de bugs | 🟢 | **Completada.** `doc/specs/PRD-modulo-produccion.md`: relevamiento contra la base de desarrollo y `develop-v3` — objetivo, clientes objetivo, trazabilidad (DAG `lotes_hijos`), etapas, lotes, recipientes, fraccionamiento, integración ALM (4 niveles) y TAR (incompleta), reporte de trazabilidad, diccionario de las 19 tablas, superficie de API, 12 diagramas Mermaid. Registra 8 bugs y 14 ítems de deuda técnica verificados — **documentados, no corregidos**. Hallazgos 🔴: anulación de lote vacía recipientes compartidos; `prd.fraccionamientos`/`prd.ingresar_deposito` invocados pero inexistentes; trazabilidad sin filtro `empr_id` | `docs/PRD-spec-modulo-produccion` — PR abierto, sin mergear |
| E7-INFRA-03 (Bloque 2) | Checklist de config de identidad (ADR-008/009) en la VM de GCP | 🟡 | **Completada, recién landeada en `develop-v3` acá** (ver Decisiones recientes — el PR #421 se había mergeado por error contra una rama ya fusionada, no contra `develop-v3`). Nueva §7 en `deployment-gcp.md`. Motivada por una pregunta directa de Rodolfo: aclara que `deployment.toml` NUNCA viaja con el código, y que además hace falta desplegar `traz-comp-dnato` `develop-v3` en el server de Dnato (§7.0-bis, con comandos concretos) | `docs/land-e7-infra-03-identidad-checklist` — este PR |
| E7-INFRA-05 (3.5) | Desplegar la fachada MCP al server GCP — checklist + artefactos (Bloque 3, tarea 3.5) | 🟡 | Completada y mergeada | PR #420 |
| E2-MCP-13 (3.4) + fixes | OpenAPI unificada + Virtual MCP Server único + smoke test real + fixes de docs (case_id, Key Manager, paths reales) | 🟡 | Completada y mergeada — verificado en DEV con smoke test real end-to-end (9/9 tools, aislamiento, Bonita real) | PR #414, #416, #417, #418, #419 |
| E1-ALM (3.3) | Sumar almacenes a la fachada (Bloque 3, tarea 3.3) | 🔴 | Completada y mergeada | PR #413 |
| E2-MCP-12 | Migrar las 5 tools de mantenimiento a `toolsMCPAPI` (Bloque 3, tarea 3.2) | 🟡 | Completada y mergeada | PR #411 |
| E2-MCP-11 | Verificar ruteo múltiple + esqueleto de la fachada `toolsMCPAPI` (Bloque 3, tarea 3.1) | 🔴 | Completada y mergeada | PR #410 |
| ADR-013 | Unificación MCP: fachada delgada `toolsMCPAPI`, un solo Virtual MCP Server | — | Aprobado y mergeado | PR #408, `sprint-3-prompts.md` en PR #409 |
| E2-MCP-10-RELEV | Relevamiento para unificación de Virtual MCP Servers (Bloque 0) | 🟢 | Completada y mergeada | PR #407 |
| E7-INFRA-01/02 | Sizing VM GCP + scripts instalación nativa WSO2 (ADR-011) | 🟡 | Completada y mergeada — VM funcionando end-to-end | PR #403 y #404 mergeados |
| ADR-012 | Aislamiento y alcance de Almacenes | — | Aprobado y mergeado | PR #405 |
| E1-API-20 | Relevamiento de estado para dimensionar Sprint 3 (3 frentes) | 🟢 | Completada | `docs/e1-api-20-sprint3-relevamiento-estado` — PR pendiente de abrir (Bloque 1 de `sprint-3-prompts.md`, no ejecutado todavía) |

### Próxima acción

**Bloque 2 y Bloque 3 completos del lado de Claude Code** (checklists preparados, nada ejecutado — ambos requieren la VM de GCP, que solo tiene Rodolfo). Falta la ejecución manual, en este orden:
1. `deployment-gcp.md` §7 (E7-INFRA-03): desplegar `traz-comp-dnato` `develop-v3` en el server de Dnato de ese proyecto (§7.0-bis, comandos concretos), completar la URL real del JWKS, confirmar los puntos de §7.2, aplicar `[[apim.jwt.issuer]]` en el `deployment.toml` de la VM.
2. `deployment-gcp.md` §6 (E7-INFRA-05): desplegar el CAR, publicar API+MCP Server, suscribir la app, verificar OAuth y aislamiento contra `mcp.cloudtrazalog.com`. Los pasos de deploy del CAR y publicación de API/MCP Server (§6.1-§6.3) no dependen de §7 y se pueden hacer antes o en paralelo — solo la verificación real (§6.4-§6.5) necesita la identidad ya aplicada.

En paralelo siguen pendientes: Bloque 1 (PR de relevamiento, no abierto todavía), y las preguntas abiertas de `doc/v3/sprint-3-relevamiento-estado.md`.

### Decisiones recientes (últimas 5)

| Fecha | Decisión | Referencia |
|---|---|---|
| 2026-08-11 | **✅ `alm_get_stock` devolviendo datos reales desde Claude, end-to-end contra GCP.** Barra de acero 50.0, Barra de acero cortada 307.0, Eje 51.0 — coincide con lo que muestra la app v2. Cierra el circuito completo: OAuth 2.1 PKCE (Dnato) → APIM (`mcp.cloudtrazalog.com`) → MI → `toolsMCPAPI` → `toolsALMAPI` → DataService → PostgreSQL. **Corrección de un diagnóstico previo mío:** llegué a afirmar que el CAR pisaba el datasource y lo dejaba apuntando a desarrollo (`10.142.0.13`); Rodolfo objetó que esa IP no es alcanzable desde la VM de GCP y tenía razón — el datasource apuntaba correctamente a `10.142.0.11`. Lo di por probado sin verificarlo en la VM. La causa real del vacío fue que la copia del repo en el servidor de demo no estaba actualizada. Lección incorporada al doc: verificar el datasource **desempaquetando el `.car` desplegado**, y comparar el nombre de la base además del host (en `10.142.0.11:5434` conviven varias bases con esquema equivalente) | `doc/mcp/verificacion-queries-datos-reales.md` §1 |
| 2026-08-11 | **4 bugs de queries encontrados y corregidos verificando las 9 tools contra datos reales.** (1) 🔴 `alm_get_stock` sumaba stock de otras empresas — `getArticulos2` unía `alm_lotes` sin filtrar por empresa: la empresa 87 reportaba 5982 cuando 3001 eran de la empresa 1. (2) `man_get_equipos` ocultaba equipos con catálogos incompletos por usar `INNER JOIN` a los 6 catálogos descriptivos: 2 equipos de la empresa 8, ambos en estado `RE`, no aparecían. (3) `man_get_ots` perdía y duplicaba a la vez (INNER JOIN a equipos descartaba solicitudes con `id_equipo=-1`; el join a `orden_trabajo` duplicaba porque el modelo admite N OT por `case_id`). (4) `man_get_ot` duplicaba por lo mismo. Verificación final: **23/23 completitud y 19/19 aislamiento en verde**. Scripts reutilizables en `scripts/dev/verify-mcp-{queries,isolation}.py` (extraen el SQL de los `.dbs`, sirven para CI) | `doc/mcp/verificacion-queries-datos-reales.md`, `ALMDataService.dbs`, `MANDataService.dbs`, `MANEquiposDataService.dbs` |
| 2026-08-11 | **✅ MCP Server de GCP funcionando end-to-end desde Claude.** `alm_get_stock` → `{"materias":{}}` y `man_get_equipos` → `{"equipos":{}}`, ambas HTTP 200 con la estructura correcta. La cadena completa quedó verificada: OAuth 2.1 PKCE contra Dnato → APIM gateway (`mcp.cloudtrazalog.com`) → MI → `toolsMCPAPI` → `toolsALMAPI` → DataService → PostgreSQL. **Lo que falta es data, no integración**: no hay stock cargado para la empresa de prueba y `empr_id_mysql` sigue en NULL. Último gotcha resuelto: el `endpointConfig` del MCP Server no se puede editar desde la UI del Publisher en 4.6.0 — hay que hacerlo por REST API y desplegar revisión (§6.3-ter). Y varios "fallos" reportados eran en realidad el MI arrancando (~40 s tras el redeploy del CAR) | `doc/v3/deployment-gcp.md` §6.3-bis/§6.3-ter, DoD de E7-INFRA-05 |
| 2026-08-11 | **Bug 🔴 de aislamiento encontrado y corregido: las 4 tools `alm_*` pasaban `jwt_mysql_empresa_id` (id de `assetv2`/MySQL) a consultas que corren contra PostgreSQL.** `ALMDataService` usa `ToolsDataSource` (Postgres) y filtra por `alm.alm_lotes.empr_id` = el id de `core.empresas` (ADR-012 §52-54). Copy-paste del patrón de las tools `man_*`, que sí deben usar el id de MySQL. **No se detectó en el smoke test de DEV porque la empresa de prueba tenía `empr_id = empr_id_mysql = 1`** — el bug es invisible cuando ambos ids coinciden. Apareció en GCP con `empr_id=9000` y `empr_id_mysql` en NULL. Riesgo que cierra: con `empr_id_mysql` poblado con un valor que exista como `empr_id` de otra empresa en Postgres, las tools `alm_*` habrían devuelto **datos de otra empresa en silencio**. Corregido a `jwt_empr_id` en las 4, con el comentario explicativo que faltaba. Aprobado por Rodolfo. Build verificado | `toolsMCPAPI.xml`, PR de esta rama |
| 2026-08-11 | **OAuth discovery resuelto en GCP — la cadena RFC 9728 → 8414 → 7591 responde en verde.** Ejecutados los 4 gaps de APIM (CAR de discovery, system properties, `https_endpoint`, `[[apim.jwt.issuer]]` ya estaba). Gotchas nuevos documentados: `carbonapps/` no existe en una instalación limpia del APIM (hay que crearlo, sí viene de fábrica en el MI); arrancar el APIM con `api-manager.sh` en vez de systemd deja el proceso **sin** las system properties (el PRM sale con los campos vacíos — buen síntoma de diagnóstico); y un proceso huérfano corriendo como root bloquea el arranque por systemd (`PID file is not owned by root. Refusing`). Del lado de Dnato, el bug #4 de §7.0-quater (rutas `oauth/*`) seguía presente: se había dado por corregido verificando solo el JWKS, sin probar la AS metadata ni `/oauth/register` | `doc/v3/deployment-gcp.md` §6.2-bis/§6.2-ter |
| 2026-08-11 | **Descubierta una SEGUNDA instancia de WSO2 que nadie había documentado: el backend de Dnato en `localhost:8283` (VM `vm-demo-trazalog`), distinta del MI de la fachada MCP (`8290`, VM `mcp-trazalog`).** El login OAuth de Dnato no pasa por la fachada MCP — llama a su propio backend. Actualizar el CAR en la VM del MCP no actualiza ese otro, que quedó viejo y no expone `/empresa/{empr_id}` (existe en `develop-v3`: `COREDataService.dbs:303` y `:820`). Además **falta la columna `empr_id_mysql` en `core.empresas`, y no existe ninguna migración SQL para ella en NINGÚN repo** — se asume en toda la cadena de ADR-009 pero nunca se formalizó. Bloqueante para PROD, candidato a issue propio. Nueva §7.0-quinquies | `doc/v3/deployment-gcp.md` §7.0-quinquies |
| 2026-08-11 | **Confirmado que los grupos de Bonita SÍ usan el formato `{empr_id}-{nombre}`** (`"name":"9000-Termotanques"` en el log del login real) — cierra la duda que había quedado abierta en §7.0-quater. `Oauthlogin.php` (que espera ese formato) está bien; el bug es de `Cli.php`, que aplica el mismo parseo contra `seg.memberships_users.group`, donde el prefijo no existe | `doc/v3/deployment-gcp.md` §7.0-quater |
| 2026-08-10 | **Gap crítico encontrado en el checklist de E7-INFRA-05: faltaba TODO el bloque de OAuth discovery del APIM.** Al intentar dar de alta el conector en Claude.ai contra la VM de GCP, el flujo OAuth fallaba con un 404 en `https://mcp.cloudtrazalog.com/authorize`. Causa raíz: `deployment-gcp.md` §6 documentaba solo el `.car` del **MI** (`ToolsAPIProject`) y omitía por completo el **segundo CAR, el del APIM** (`trazalog-discovery-1.0.0.car`, de `_backend/api/ApimDiscoveryProject`), que sirve el PRM RFC 9728 y es el que le dice a Claude que el Authorization Server es Dnato. En DEV nunca se notó porque `scripts/dev/setup-ngrok.sh` lo despliega solo (paso 3b) junto con las system properties, el `https_endpoint` y el issuer del Key Manager — todo automático, y por eso nunca se documentó como paso manual. 4 gaps de APIM (CAR ausente, `JAVA_OPTS` sin las system properties, `https_endpoint` en `localhost:8243`, Key Manager anunciándose a sí mismo como AS en violación de TAD-IDENT-04). Del lado de Dnato **no faltaba nada**: G2/G3 de `oauth-discovery-flow.md` ya están implementados en `develop-v3` (`Oauth.php::register_client()`, `routes.php:66`). Nueva §6.2-bis (procedimiento) y §6.2-ter (troubleshooting de los síntomas reales en Claude); `install-apim.sh`, `wso2am.service` y `.env.example` actualizados para que una VM nueva lo haga solo | `doc/v3/deployment-gcp.md` §6.2-bis/§6.2-ter, `deploy/gcp/` |
| 2026-08-09 | **§7 de `deployment-gcp.md` reescrita con troubleshooting real completo**, ejecutando el checklist de identidad de punta a punta contra `demo.cloudtrazalog.com`. Encontrados y resueltos en vivo: cadena TLS incompleta (intermedio Sectigo mal configurado en Apache + raíz faltante en el trust store del server, viejo/CentOS 7 EOL — troubleshooting completo en §7.0-ter), y 6 bugs de `traz-comp-dnato` (§7.0-quater): `composer_autoload` en `FALSE`, `permitted_uri_chars` sin `@`, sintaxis `??` (PHP7+) en `Cli.php` corriendo sobre PHP 5.6, rutas de `oauth/*` faltantes en `routes.php`, ruta relativa en `JWT_PUBLIC_KEY_PATH` del `.htaccess`, y resolución de `empr_id` en `Cli.php` por parseo de prefijo en vez de lookup a `core.empresas` (confirmado por Rodolfo como bug real, no solo de este server). Los 4 bugs de código (no el de `empr_id`, que es de lógica/datos) quedan marcados como candidatos a PR en `traz-comp-dnato` — si PROD se despliega con `git clone` en vez de copiar archivos a mano, se pisan contra los mismos | `doc/v3/deployment-gcp.md` §7.0-ter, §7.0-quater |
| 2026-08-08 | **Corrección en §6.1/§6.2 de `deployment-gcp.md`: usar `mvn clean install` (Maven del sistema), no `./mvnw`.** Rodolfo, ejecutando el checklist en la VM Rocky Linux, encontró "comando desconocido" al correr `./mvnw`. Causa raíz: `mvnw`/`mvnw.cmd`/`.mvn/wrapper/maven-wrapper.jar` están en `.gitignore` de `ToolsAPIProject` — nunca se commitearon, así que un `git clone`/`git pull` fresco (como el de la VM) nunca los trae. La verificación "BUILD SUCCESS" documentada en §6.1 se había hecho contra un `./mvnw` local que no viene de git. Confirmado que Maven del sistema (3.8.7) coincide con lo que pide `maven-wrapper.properties` | `doc/v3/deployment-gcp.md` §6.1-§6.2 |
| 2026-08-08 | **PR #421 (E7-INFRA-03) se había mergeado contra la rama equivocada — reconciliado acá.** Lo abrí con base `feature/e7-infra-05-deploy-mcp-facade-gcp` (PR #420) en vez de `develop-v3`, para que el diff quedara limpio. Cuando #420 se mergeó primero (squash) y recién después se aprobó/mergeó #421, el squash de #421 quedó aplicado sobre esa rama feature, no sobre `develop-v3` — §7 nunca llegó a integración. "Squash and merge" siempre mergea contra el base branch que tenía el PR al momento de mergear, no contra `develop-v3` por default. Reconciliado con un merge manual (`git merge origin/feature/e7-infra-05-deploy-mcp-facade-gcp` sobre `develop-v3` actualizado) en este PR | `doc/v3/STATE.md`, `doc/v3/deployment-gcp.md` |
| 2026-08-08 | **Corrección en §7: SÍ hace falta desplegar `traz-comp-dnato` en el servidor de Dnato de GCP.** Rodolfo repreguntó tras ver commits de identidad en GitHub que la sección anterior decía que no hacían falta. Verificado directamente en `traz-comp-dnato` (clon local): los commits de JWT/OAuth 2.1 PKCE/JWKS (E9-IDENT-03/04) están SOLO en `develop-v3` de ese repo, no en `develop` ni `master` — si el servidor de Dnato de ese proyecto GCP corre otra rama, no tiene ese código. Agregado §7.0-bis con el checklist real (deploy de `develop-v3`, generar claves RS256 en ese server, migración DB) — trabajo de `traz-comp-dnato`, solo señalado acá como dependencia | `doc/v3/deployment-gcp.md` §7.0-bis |
| 2026-08-08 | **Tarea 3.5 completada (checklist, no ejecución — requiere acceso a la VM que solo tiene Rodolfo).** `deployment-gcp.md` §6 nueva: despliegue del CAR, publicación de API+MCP Server, verificación OAuth y aislamiento contra `mcp.cloudtrazalog.com`. Incorpora todos los gotchas reales encontrados el mismo día en el smoke test de DEV para que no se repitan en GCP. Flagea explícitamente la dependencia con E7-INFRA-03 (identidad) para el paso de aislamiento | `doc/v3/deployment-gcp.md` §6 |
| 2026-08-08 | **Smoke test real del MCP Server (:8243) completado, a pedido de Rodolfo.** 9/9 tools OK, aislamiento OK, escrituras con Bonita real OK. Encontrados y corregidos en vivo 2 bugs de configuración que bloqueaban todo (suscripción faltante de la app `TrazalogDnatoMCP` al MCP Server nuevo, y endpoint mal configurado en API y MCP Server — apuntaba a `10.142.0.13:8280`, dato incorrecto mío desde la Tarea 3.4, corregido a `localhost:8290`). Documentado como paso nuevo (§2.8-bis) y troubleshooting en `virtual-mcp-unificado.md` para que no se repita | `doc/mcp/virtual-mcp-unificado.md` |
| 2026-08-08 | **Paths reales del MCP Server confirmados**: Rodolfo publicó `Trazalog MCP Server`, contexto `/trazalog/mcp`, versión `1.0` — no `trazalog-operaciones`/`/trazalog-operaciones` como sugería ADR-013. Resuelve la pregunta abierta de nombre del ADR. Docs y spec actualizados; tests Hurl no necesitaron cambios (apuntan al MI, capa sin tocar) | `doc/mcp/virtual-mcp-unificado.md`, `doc/api/trazalog-operaciones.yaml`, `doc/adr/ADR-013-unificacion-mcp.md` |
| 2026-08-08 | **Corrección en `virtual-mcp-unificado.md`**: la sección de seguridad OAuth2 (§2.4) decía que había que seleccionar "Dnato" como Key Manager en el Publisher y des-seleccionar Resident KM — instrucción incorrecta escrita en la Tarea 3.4, sin cruzar contra el doc canónico de identidad. Bloqueó a Rodolfo al intentar publicar `trazalog-operaciones` (esa opción no existe: Dnato se valida vía `[[apim.jwt.issuer]]`, no como Key Manager). Corregido con el procedimiento real de `apim-keymanager-dnato.md` §5: no tocar el selector de Key Managers, desactivar `Enable Subscription Validation` | `doc/mcp/virtual-mcp-unificado.md`, `doc/identity/apim-keymanager-dnato.md` |
| 2026-08-08 | **Prueba de humo de E2-MCP-13 en DEV, a pedido de Rodolfo.** 9/9 tools verificadas con datos reales (empr_id=1) contra el MI local con VPN+Postgres arriba. Encontrado y arreglado (Rodolfo pidió el fix directo: "el arreglo del case_id es simple, lo podrias ejecutar por favor") un bug preexistente en `MANDataService.dbs`: `case_id` se leía del LEFT JOIN a `orden_trabajo` en vez de la tabla base `solicitud_reparacion`, dando `null` en OTs recién creadas | `fix/e2-mcp-case-id-null-man-queries`, `MANDataService.dbs` |
| 2026-08-08 | **Doc de Ambientes creado**, a pedido de Rodolfo, tras confundirse con un redirect a ngrok al entrar a Publisher. Documenta cuándo DEV necesita ngrok (testing MCP con Claude.ai, federación Dnato) y cuándo no (trabajo diario), más el estado y acceso de TEST/PROD. Se identificó y corrigió la causa raíz del problema puntual: `hostname` en el `deployment.toml` local quedó pisado con un subdominio ngrok muerto por no haber corrido `--reset` al cerrar sesiones anteriores | `doc/infra/ambientes.md`, `scripts/dev/setup-ngrok.sh` |
| 2026-08-03 | **Tarea 3.4 completada.** OpenAPI unificada de las 9 tools + doc de Virtual MCP Server único con guía de migración coordinada. Detectada y corregida staleness en `openapi-publish-procedure.md` (describe `X-Empr-Id` en vez del `X-JWT-Assertion` vigente de ADR-009) — no corregida en el doc viejo, solo evitada en el nuevo | `doc/api/trazalog-operaciones.yaml`, `doc/mcp/virtual-mcp-unificado.md` |
| 2026-08-02 | Rodolfo confirmó el nombre real del proceso Bonita para `alm_crear_pedido_materiales`: **"Pedido de Recursos Materiales"** — la etiqueta `"Ped. Materiales"` de `constants.php` (usada como suposición inicial) NO era el nombre real registrado en Bonita. Corregido en `toolsALMAPI.xml` y en el test Hurl | `toolsALMAPI.xml`, `tests/security/mcp-facade-alm-tools.hurl` |
| 2026-07-31 | **Tarea 3.3 (almacenes) completada y mergeada.** `toolsALMAPI.xml` (nuevo, contexto `/tools/alm`) como capa de orquestación que recibe `empr_id` EXPLÍCITO (no derivado de JWT) — decisión de Rodolfo, reusable a futuro por el PHP de v2. `toolsMCPAPI` expone 4 tools `alm_*` como fachada delgada. 3 queries aditivas en `ALMDataService.dbs`. Rollback de 3 niveles | PR #413, `toolsALMAPI.xml`, `toolsMCPAPI.xml`, `ALMDataService.dbs` |
| 2026-07-31 | E2-MCP-12 completada y mergeada: las 5 tools de mantenimiento en `toolsMCPAPI` con prefijo `man_`, ninguna reimplementa lógica | PR #411 |
| 2026-07-31 | E2-MCP-11 mergeado (PR #410): ruteo múltiple a distintos DataServices desde una sola API MI confirmado | PR #410 |

### Bloqueos

- ~~OAuth discovery del APIM~~ **Resuelto 2026-08-11** — la cadena de discovery responde en verde (§6.2-bis).
- **Los datasources viven embebidos en el `.car`, con credenciales en texto plano en el repo.** Cada instancia necesita su propia copia del repo con las URLs de su ambiente, y un checkout desactualizado en el servidor produce un CAR con la config equivocada (pasó el 2026-08-11 en demo). **Mejora pendiente de decisión (🔴)**: moverlos a `[[datasource]]` del `deployment.toml` del MI — config por ambiente y sin secretos en el artefacto. Ver `doc/mcp/verificacion-queries-datos-reales.md` §1.
- **16 queries de `ALMDataService` que usa el PHP de v2 tienen el mismo bug de stock cross-empresa** que se corrigió en `getArticulos2` (join a `alm_lotes` sin filtro de empresa). No se tocaron por estar fuera del alcance MCP y no poder probarse sin la app v2 — pero si el stock que muestra v2 sale de ahí, tiene el mismo sesgo. Ver §6 de ese mismo doc.
- **`empr_id_mysql` no tiene migración SQL en ningún repo** — la columna se asume existente en `core.empresas` (usada por `COREDataService.getEmpresaById`/`updateEmpresaAssetId` y por toda la cadena de identidad de ADR-009), pero nunca se formalizó como migración. Faltaba en la base de TEST y hubo que agregarla a mano. **Bloqueante para el despliegue a PROD** si no se crea la migración antes. Ver §7.0-quinquies.
- **El WSO2 backend de Dnato (`vm-demo-trazalog:8283`) está desactualizado** respecto de `develop-v3` — no expone `/empresa/{empr_id}`. Es una instancia distinta del MI de la fachada MCP; redesplegar el CAR ahí (no parchear el `.dbs` a mano, se pierde en el próximo deploy). Ver §7.0-quinquies.
- **El checkout de `traz-comp-dnato` en el server de demo está desincronizado de `develop-v3`** — van 3 archivos con código faltante que sí existe en el repo (`routes.php`, `Oauthlogin.php`, `Empresas.php`). Conviene sincronizar el checkout completo en vez de seguir parcheando de a uno. Ver §7.0-quater.
- **Antes de ejecutar los pasos de consola de `virtual-mcp-unificado.md`:** confirmar que `apim.jwt.enable=true` está activo en el `deployment.toml` del APIM (debería estarlo ya, si `trazalog-equipos`/`trazalog-ots` funcionan hoy) — sin eso, el backend JWT `X-JWT-Assertion` no se genera y `EmprIdFromHeader` falla con `identity_missing` para toda la API nueva.
- **Confirmado (2026-08-08, smoke test real): WSO2 4.6.0 NO lee `x-mcp-annotations` del OpenAPI** — `readOnlyHint`/`destructiveHint` no aparecen en el `inputSchema` de `tools/list`. Gap preexistente de Sprint 2 (`equipos.yaml`/`ot.yaml` tampoco la usaban), no introducido por esta tarea, no bloqueante — Claude igual sabe cuándo pedir confirmación por la descripción semántica de cada tool.
- Config de identidad (ADR-008/009) y migración de DataServices a PostgreSQL siguen pendientes antes de poder dar de alta al primer cliente sobre la VM de GCP — cubierto por E7-INFRA-03 (Bloque 2, no ejecutado todavía).
- **Nota operativa (de E2-MCP-11, sigue vigente):** el deploy del `.car` de `ToolsAPIProject` es atómico — si `ALMDataService` (o cualquier otro `.dbs`) no puede conectar a su BD al momento del deploy, se revierte el CAR completo.
- La implementación de escritura MCP en Almacenes sigue bloqueada hasta que el PM confirme cómo resolver el aislamiento multi-tenant de `ALMDataService` (ver pregunta abierta #1 del relevamiento de Sprint 3) — posible tema de arquitectura (🔴).
