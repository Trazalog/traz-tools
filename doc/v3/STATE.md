# STATE.md — Estado vivo del proyecto Trazalog v3

> **Este archivo vive en `traz-tools/doc/v3/STATE.md`.** Es el ÚNICO documento de estado del proyecto — reemplaza kickoffs-como-estado, backlog xlsx, y resúmenes pegados al chat.
>
> **Por qué solo en traz-tools y no duplicado en traz-comp-dnato:** el estado de un proyecto con dos repos necesita una sola fuente para no repetir el problema de desincronización que motivó esta metodología (dos copias = deriva garantizada). Se eligió `traz-tools` como master porque concentra más volumen de trabajo (WSO2/APIs/MCP) y es donde vive el Project board de GitHub. `traz-comp-dnato/CLAUDE.md` tiene una línea que apunta acá (ver bloque E4).
>
> **Reglas de actualización:**
> - Claude Code actualiza este archivo al final de CADA tarea (🟢🟡🔴), como parte de su Definition of Done. No es opcional.
> - Claude Web lo lee al inicio de cada sesión, antes de asumir cualquier estado.
> - Rodolfo + Claude Web lo revisan juntos en el ritual semanal (ver CICD_STRATEGY §5-bis.7) para corregir cualquier deriva.
> - Formato: mantener SIEMPRE la estructura de secciones. No agregar prosa larga — es un tablero, no una bitácora.

---

**Sprint actual:** Sprint 3 — Activación early adopter minero
**Objetivo del sprint:** Reemplazar ngrok por un despliegue estable en GCP (ADR-011) y cerrar la deuda técnica pendiente antes de activar al primer cliente early adopter.
**Última actualización:** 2026-09-02 por Claude Code (AGENTE-E0 — análisis de alertas de AssetPlanner entregado; **frente nuevo Agente Minero v3.5**, rama base `develop-v3.5` creada desde `develop-v3`; esperando gate 1 del PM)
**Última actualización:** 2026-08-21 por Claude Code (MCP-REPUBLICACION — cerrada, 14/14 end-to-end; antes — 17/17 tools operativas; recrear la API es el paso que resuelve; antes MCP-KPIS — 4 tools de KPI; antes MCP-PUBLICACION — integración de las tools nuevas sobre `develop-v3` para republicar el MCP Server)
**Última actualización:** 2026-08-20 por Claude Code (MCP-REPUBLICACION — 17/17 tools operativas; recrear la API es el paso que resuelve; antes MCP-KPIS — 4 tools de KPI; antes MCP-PUBLICACION — integración de las tools nuevas sobre `develop-v3` para republicar el MCP Server)
**Última actualización:** 2026-08-15 por Claude Code (DOC-MAN-KPIS — hallazgos sobre los KPIs de mantenimiento verificados contra producción)
**Última actualización:** 2026-08-19 por Claude Code (DOCTEST-F1 — relevamiento de DNATO, 2da pasada con las respuestas del PM; sigue en gate de validación)

### Tareas activas

| ID | Descripción | Clase | Estado | Rama / PR |
|---|---|---|---|---|
| AGENTE-SI-01 | Instalar WSO2 Streaming Integrator en la máquina de desarrollo — primer paso de la Opción C decidida en AGENTE-E0 | 🟢 | **Completada y verificada end-to-end.** SI 4.4.0 corriendo con **JDK 21** (Temurin), offset de puertos 10 para no chocar con el 9443 de APIM, drivers PostgreSQL y MySQL convertidos a bundles OSGi, y CDC polling probado contra `assetv2` (fila insertada, capturada por el Siddhi con su `data_json`, y borrada — la tabla quedó igual que antes). **Tres trampas que costaron el rato y quedaron documentadas en `doc/agente/instalacion.md`:** (1) el `siddhi-io-cdc-2.1.1.jar` de la distro **viene sin `META-INF/MANIFEST.MF`** —también el de Maven Central—, así que el source `cdc` no carga y toda app que lo use falla con `No extension exist for source:cdc`; se arregla agregando un manifest mínimo y pasándolo por `jartobundle.sh`. (2) `extension-installer.sh install cdc` no sirve: el catálogo de 4.4.0 no incluye CDC. (3) el CLI del installer tiene el puerto hardcodeado y no lee el offset — hay que ajustar su `config.properties` a 9100 o falla con conexión rehusada. Además hubo que liberar disco: la raíz estaba al 99% | `feature/agente-e0-analisis-alertas` → PR #498 |
| AGENTE-E0 | **Frente nuevo: Agente Minero v3.5.** Análisis del mecanismo de notificaciones de AssetPlanner como insumo del puente de alertas del agente (ADR-A6). Rama base `develop-v3.5` creada desde `develop-v3`; los 3 documentos ancla (`AGENTE_MINERO_ARQUITECTURA_FUNCIONAL/TECNICA`, `PROMPT_CC_AGENTE_MINERO`) entraron a `doc/v3/` — antes vivían fuera de git | 🟢 | **Completada, en PR — esperando gate 1 del PM.** `doc/agente/analisis-alertas-assetplanner.md`. **Hallazgo central: AssetPlanner no tiene un sistema de alertas configurable, tiene un pipeline de push cableado por triggers de MariaDB.** No hay reglas, ni preferencias por usuario, ni canal de mail, ni destinatario por rol. Sí es reutilizable el `FirebaseConnectorAPI` del MI (que ya vive en este repo), el modelo de cola, el service worker y el patrón de campanita. **Tools no tiene nada**: `traz-comp-notificaciones` es un esqueleto con código de ejemplo de subida de imágenes. **Gate 1 resuelto: el PM eligió la Opción C** — cola propia en PostgreSQL, app Siddhi nueva sobre el Streaming Integrator y el `FirebaseConnectorAPI` existente, sin tocar `assetv2`. El SI se conserva como **pieza estándar de la arquitectura** por la extensibilidad futura de las alarmas, y el orquestador solo encola. ADR-A6 queda respetado en su letra. **Instalar el SI está bloqueado por espacio en disco** (ver Bloqueos). Verificado contra la base de desarrollo: 434 filas en la cola, **0 procesadas** desde 2024 | **PR #498** — `feature/agente-e0-analisis-alertas` → `develop-v3.5` |
| DOCTEST-DOCS | Incorporar al repo los 3 documentos ancla de DocTest (la especificación aprobada de la solución de catálogo funcional → ayudas + tests). Hasta ahora vivían fuera de git y entraban sueltos en el working tree, con riesgo de commitearse en ramas ajenas | 🟢 | **Completada, en PR.** Doc 1 v1.1, Doc 2 v1.1 y Doc 3 v1.2 — versiones verificadas contra el encabezado de cada archivo antes de commitear. Son la **fuente única**: cualquier prompt o resumen que los contradiga pierde. La implementación va aparte, por fases (F0 #437 … F5 #442) | **Mergeada** — PR #446 |
| DOCTEST (Ola 1) | Implementar DocTest — catálogo funcional versionado del que se derivan ayudas de usuario, tests automatizados (Playwright E2E + Hurl MCP) y docs Gherkin. Especificación aprobada en `doc/v3/TRAZALOG_v3_DOCTEST_01/02/03` (entran al repo por el PR #446 — DOCTEST-DOCS, precondición de todo lo demás). Plan en 6 fases con un issue por fase: **F0 infraestructura (#437) · F1 DNATO (#438) · F2 MAN piloto (#439) · F3 ALM (#440) · F4 MCP (#441, paralelizable) · F5 CI/CD (#442)** | 🟢 F0 / 🟡 F1-F5 | **F0 mergeada (PR #443)**: árbol `doctest/` según Doc 3 §2, Playwright + TS con projects `local`/`demo`/`staging-v3`, Hurl 8.0.1, JSON Schema + `validate-catalog.ts` (reglas R1-R6 con selftest), workflow `doctest-validate.yml` — primer workflow del repo. **F1 COMPLETA** (2026-08-25): 28 casos de DNATO relevados y validados por el PM (25 validados, 2 en borrador por definición pendiente, 1 obsoleto), 25 `.feature` derivados, **55 tests E2E en verde** (smoke 1m37, suite completa 7,3 min), la ayuda de usuario generada con la plantilla del sitio real, y el buscador del inicio indexando las 26 secciones de todos los manuales. Dos empresas de test creadas por el alta real. **La suite encontró 6 bugs que ninguna lectura de código había visto** (#463 a #468). **F3 (ALM) es lo próximo** | F0: PR #443 · F1: PR #450 mergeado + PR #469 |
| REQ-ASSET-ALM | Requerimiento del PM (cliente en el corto plazo): asset deja de usar su almacén y herramientas propios y consume los de tools vía REST (`ALMDataService`/`PANDataservice`), sin migración masiva de datos. Adelanta la Etapa 5 del plan de migración. **El estado por fases (F0-F6) vive en `traz-prod-assetplanner/doc/v3/STATE.md`** — este tablero solo registra el hito | 🟡 | **F0 en PR** (metodología en el repo de asset: `develop-v3` creada, CLAUDE.md + CONTEXT-PACK + STATE). F1 (sincronizar `ALMDataService` EI←MI + `getEmpresaByMysqlId`) es la próxima y corre en ESTE repo | asset PR #322; verificación de datos ver Decisiones |
| DOC-MAN-MIGRACION | Relevamiento y plan de migración de AssetPlanner (`traz-prod-assetplanner`) al submódulo `traz-tools-man` — pedido ad-hoc del PM para retomar el trabajo abandonado en 2024 | 🟢 | **Completada.** `doc/migracion/assetplanner-a-traz-tools-man.md`: estado artefacto por artefacto del fork de Rogelio Sanchez (43 archivos / 36.122 LOC en la rama `rnsanchez`, nunca integrada — el submódulo apunta a "First commit"), mecánica de migración reconstruida con evidencia, tabla de divergencia post-corte (sólo 15 de 43 archivos cambiaron en 20 meses), 8 riesgos de proceso + 10 técnicos, plan de pruebas en 6 niveles, y estrategia en 6 etapas con CI/CD y corte por empresa. **Documenta, no corrige.** Hallazgos 🔴: el módulo no arranca (62 queries de controllers pegan a PostgreSQL en vez de MariaDB, 89 accesos de sesión a una clave que el host nunca escribe, 23 vistas referenciadas ausentes); fuga de aislamiento multi-tenant por `OR` sin paréntesis en `Equipos.php:830-832`; ~15 queries con SQL injection; bucle de recurrencia sin límite ni transacción; credenciales y un JWT en claro en el repo | `docs/migracion-assetplanner` — PR abierto, sin mergear |
| REQ-ASSET-ALM **F1** | Sincronizar `ALMDataService` EI←MI + lookup inverso de empresas (`getEmpresaByMysqlId`) + migración formal de `empr_id_mysql`. Prerrequisito de F3-F5 del requerimiento (el plan por fases vive en `traz-prod-assetplanner/doc/v3/STATE.md`) | 🟡 | **Completada, en PR.** Las dos copias quedaron con 84 queries idénticas: EI recibió las 4 del MI (pedidos/caseid/depositos MCP) y la parametrización de `getArticulo`/`getArticulos2` (tenían `empr_id = 1` HARDCODEADO); MI recibió `getHistoricoEntMateriales`. **Fix de aislamiento aplicado a las 3 queries de artículos en ambas copias** — verificado contra Postgres DEV: la empresa 87 reportaba 7.534 de stock sin el fix y 4.532 con él (3.002 unidades de otras empresas). `COREDataService` (ambas copias): query + resource `GET /empresa/porAssetId/{empr_id_mysql}`, probado contra DEV (match y no-match). `scripts/sql/2026-08-core-empresas-empr-id-mysql.sql` cierra el bloqueante "columna sin migración formal" (idempotente, unicidad parcial; DEV verificado sin duplicados — **aplicación manual pendiente en TEST/PROD**). CAR del MI buildea y empaqueta los fixes (verificado descomprimiendo el `.car`). Deuda anotada: la copia EI de CORE sigue sin `getEmpresaById`/`updateEmpresaAssetId`, y quedan queries con el bug de lotes fuera de las 3 de artículos | `feat/f1-alm-ei-sync-y-lookup-empresa` — PR abierto, sin mergear |
| MCP-PUBLICACION | Integrar las tools MCP nuevas sobre `develop-v3` para poder republicar el MCP Server y probarlo | 🟡 | **Completada, en PR.** La rama `docs/land-e7-infra-03-identidad-checklist` tenía 34 commits sin landear y estaba 9 commits atrás de `develop-v3`: se integró resolviendo 10 conflictos (4 de artefactos WSO2, todos adiciones adyacentes a los cambios de F1/F5 — verificado que `getPedidoPorOrden`/`setPedidoOrden` y el fix de `getArticulos2` sobreviven). **13 tools**, con OpenAPI e implementación cruzadas 1:1 sin desalineación. Nuevas respecto de `develop-v3`: `man_get_lecturas`, `alm_get_vencimientos`, y filtros opcionales (`depo_id`/`tipo`/`buscar`) en `alm_get_stock`. Build verde y CAR verificado por dentro (las 5 queries MCP empaquetadas). **`man_get_predictivos` y `man_get_backlog` no existen en ningún lado** — quedaron sin implementar. Hallazgo del deploy: **el `.car` empaqueta la IP de desarrollo** en los 2 datasources + `apiconfig.xml` + `bpmconf.xml`, y no hay paso automatizado que lo reemplace; documentado en `deployment-gcp.md` §6.1-bis, pendiente decidir si se parametriza Se agregó además `doc/mcp/republicar-mcp-server.md`: el procedimiento de **republicación** no existía completo —estaba en 3 documentos, y la §7 de `openapi-publish-procedure.md` omitía desplegar revisión y que el MCP Server no hereda las operaciones de la API—, que son justo los dos pasos que hacen que una tool nueva no aparezca en `tools/list` sin que nada falle | `feat/mcp-tools-publicacion` — este PR |
| DOC-MAN-KPIS | Verificar si los KPIs de mantenimiento de AssetPlanner (Disponibilidad, MTTR, MTBF/MTTF, cantidad de fallas) calculan lo que dicen calcular, y documentar los hallazgos para revisarlos con los ingenieros de mantenimiento de Tierra Capayán — pedido ad-hoc del PM | 🟢 | **Completada.** `doc/mantenimiento/kpis-consultas-a-ingenieria.md` (+PDF, 17 pág.), escrito para un ingeniero de mantenimiento y contrastado contra fuentes de ingeniería. **Hallazgo 🔴 principal, verificado en producción (empresa 15, ene-2024 a jul-2026): 17.372 solicitudes de reparación contra 4 lecturas en estado `RE`** — los KPIs leen el historial de estados del equipo, pero el trabajo real se registra por solicitudes/OTs, así que calculan sobre el 0,02% de la actividad. MTTR, MTBF y cantidad de fallas **no se arreglan ajustando la fórmula**: hay que reconstruirlos desde las OTs, y el campo `correctivo` viene NULL en las 17.372 (no se puede separar falla de preventivo). Bug puntual sí corregible: los tramos se cierran con la lectura siguiente **por `id_lectura` en vez de por fecha**, lo que invierte **4.724 de 20.868 tramos (22,6%)** — contra sólo 12 con fecha rota; Disponibilidad los descarta, MTTR/MTBF no (de ahí el MTTF de −53.328.934). La unidad divide minutos por 24 en vez de 60. Además, las lecturas generadas al cerrar OTs copian la fecha de la OT **sin validar** (802 de 966 OTs tienen `fecha_terminada` vacía). **Disponibilidad es el único KPI cuyo cálculo está bien** (99,74%), con la reserva de que opera sobre ese mismo historial. **Documenta y consulta, no corrige** — ningún cálculo fue modificado | `docs/man-kpis-hallazgos-ingenieria` — este PR |
| MCP-KPIS | Exponer los 4 KPIs de mantenimiento como tools MCP, mostrando el dato **tal cual** lo calcula el sistema (decisión del PM: documentar los hallazgos y no corregir nada por ahora) | 🟡 | **Completada, en PR.** 4 tools nuevas: `man_get_kpi_disponibilidad`, `man_get_kpi_mttr`, `man_get_kpi_mttf`, `man_get_kpi_fallas`. Las queries ya existían en `MANDataService.dbs` con sus resources REST — **no se tocó ninguna**, las tools sólo las exponen, así que el número coincide 1:1 con el tablero de AssetPlanner. `fec_inicio`/`fec_fin` obligatorios (`YYYY-MM-DD`), con corte y error explicativo si faltan; empresa siempre del JWT (`jwt_mysql_empresa_id`, id nativo de assetv2). La descripción de cada tool en la OpenAPI remite a `doc/mantenimiento/kpis-consultas-a-ingenieria.md` y advierte no presentar el número como verdad operativa sin esa lectura. **17 tools** en total, OpenAPI e implementación 1:1. Build verde, CAR verificado. Origen: el PM no encontraba las operaciones de KPI en el YAML durante la republicación — no existían, por una interpretación equivocada mía de "los MCP deben mostrar el dato tal cual" | `feat/mcp-tools-kpi` — este PR |
| MCP-REPUBLICACION | Publicar y dejar operativas las 17 tools MCP en la VM de GCP | 🟡 | **Completada — 14/14 verificadas end-to-end** con token real por el camino completo (Caddy → gateway → MI → base/Bonita), incluida la escritura con instanciación en Bonita. **Fueron tres problemas encadenados, que se veían casi igual desde afuera:** **(1)** las tools nuevas quedaban sin `apiOperationMapping` — se resuelve **recreando la API**, no el MCP Server (6 alternativas probadas y descartadas); **(2)** las tools de escritura fallaban en el cliente **aunque el dato se creara**: el MI responde `202 Accepted` y en MCP eso significa "notificación sin cuerpo", así que el cliente descarta la respuesta. **El `api.log` del APIM informaba `200` mientras el cable entregaba `202`** — sólo el access log de Caddy dice la verdad. Resuelto con un rewrite `202→200` en el `Caddyfile`; **(3)** APIM no conoce `server/discover` (protocolo `2026-07-28`) y responde `404` no-JSON-RPC. Sin conectividad a Bonita (firewall) las dos tools de escritura fallaban antes de todo eso. Pendientes menores: que el MI devuelva `200` de origen (mediación en el CAR sin efecto en runtime) y poblar `core.empresas.empr_id_mysql`, sin lo cual las 11 tools `man_*` devuelven vacío. Herramientas y guía de diagnóstico en `doc/operaciones/herramientas-mcp.md` | `docs/mcp-aprendizaje-republicacion` — PR #448 |
| MCP-REPUBLICACION | Publicar las 17 tools MCP en la VM de GCP | 🟡 | **Completada — 17/17 operativas** (verificado con `mcp-smoke-tools.py`: todas en `401`, 0 rotas). **Aprendizaje central: agregarle operaciones a una API que ya tiene un MCP Server generado no funciona por ningún camino; hay que RECREAR la API.** Lo consultable por REST (operations, definición OpenAPI, revisión desplegada) mostraba los 17 recursos, pero `findMatchingTemplate()` lee `AM_API_URL_MAPPING` y ahí las filas de las operaciones agregadas después nunca se insertaron; las tools quedaban sin `apiOperationMapping`, con `500` al invocarlas y la pantalla `Tools` del Publisher rota. **Descartado a fuerza de prueba:** desplegar revisión de la API (necesario pero insuficiente), `PUT` del mapping por REST (acepta con `200` y guarda `null`), recrear el MCP Server desde la UI, recrearlo con nombre y contexto nuevos, `apictl import --update` (`500`, bug #4997) e `import` como nuevo (descarta las tools sin mapping). Segundo hallazgo: **`Publish` no es `Deploy`** — importar la OpenAPI y publicar no lleva ningún recurso al gateway. Tres bugs de WSO2 involucrados, todos con fix mergeado río arriba y ninguno aplicable sin suscripción: [#5106](https://github.com/wso2/api-manager/issues/5106), [#4997](https://github.com/wso2/api-manager/issues/4997) y el alta desde la UI, que **no está reportado**. Procedimiento reescrito en `doc/mcp/republicar-mcp-server.md` (de bitácora a procedimiento) + 7 scripts de diagnóstico y reparación en `scripts/dev/` | `fix/docs-registry-resources-car` — PR #436 |
| DOC-PRD-SPEC | Spec funcional y técnica del módulo de Producción (PRD / `traz-prod-trazasoft`, esquema `prd`) — pedido ad-hoc del PM como base para corrección de bugs | 🟢 | **Completada.** `doc/specs/PRD-modulo-produccion.md`: relevamiento contra la base de desarrollo y `develop-v3` — objetivo, clientes objetivo, trazabilidad (DAG `lotes_hijos`), etapas, lotes, recipientes, fraccionamiento, integración ALM (4 niveles) y TAR (incompleta), reporte de trazabilidad, diccionario de las 19 tablas, superficie de API, 12 diagramas Mermaid. Registra 8 bugs y 14 ítems de deuda técnica verificados — **documentados, no corregidos**. Hallazgos 🔴: anulación de lote vacía recipientes compartidos; `prd.fraccionamientos`/`prd.ingresar_deposito` invocados pero inexistentes; trazabilidad sin filtro `empr_id` | `docs/PRD-spec-modulo-produccion` — PR abierto, sin mergear |
| E7-INFRA-03 (Bloque 2) | Checklist de config de identidad (ADR-008/009) en la VM de GCP | 🟡 | **Completada, recién landeada en `develop-v3` acá** (ver Decisiones recientes — el PR #421 se había mergeado por error contra una rama ya fusionada, no contra `develop-v3`). Nueva §7 en `deployment-gcp.md`. Motivada por una pregunta directa de Rodolfo: aclara que `deployment.toml` NUNCA viaja con el código, y que además hace falta desplegar `traz-comp-dnato` `develop-v3` en el server de Dnato (§7.0-bis, con comandos concretos) | `docs/land-e7-infra-03-identidad-checklist` — este PR |
| E7-INFRA-05 (3.5) | Desplegar la fachada MCP al server GCP — checklist + artefactos (Bloque 3, tarea 3.5) | 🟡 | Completada y mergeada | PR #420 |
| E2-MCP-13 (3.4) + fixes | OpenAPI unificada + Virtual MCP Server único + smoke test real + fixes de docs (case_id, Key Manager, paths reales) | 🟡 | Completada y mergeada — verificado en DEV con smoke test real end-to-end (9/9 tools, aislamiento, Bonita real) | PR #414, #416, #417, #418, #419 |
| E1-ALM (3.3) | Sumar almacenes a la fachada (Bloque 3, tarea 3.3) | 🔴 | Completada y mergeada | PR #413 |
| E2-MCP-12 | Migrar las 5 tools de mantenimiento a `toolsMCPAPI` (Bloque 3, tarea 3.2) | 🟡 | Completada y mergeada | PR #411 |
| E2-MCP-11 | Verificar ruteo múltiple + esqueleto de la fachada `toolsMCPAPI` (Bloque 3, tarea 3.1) | 🔴 | Completada y mergeada | PR #410 |
| ADR-013 | Unificación MCP: fachada delgada `toolsMCPAPI`, un solo Virtual MCP Server | — | Aprobado y mergeado | PR #408, `sprint-3-prompts.md` en PR #409 |
| E2-MCP-10-RELEV | Relevamiento para unificación de Virtual MCP Servers (Bloque 0) | 🟢 | Completada y mergeada | PR #407 |
| E7-INFRA-01/02 | Sizing VM GCP + scripts instalación nativa WSO2 (ADR-011) | 🟡 | Completada y mergeada — VM funcionando end-to-end | PR #403 y #404 mergeados |
| ADR-012 | Aislamiento y alcance de Almacenes | — | Aprobado y mergeado | PR #405 |
| E1-API-20 | Relevamiento de estado para dimensionar Sprint 3 (3 frentes) | 🟢 | Completada | `docs/e1-api-20-sprint3-relevamiento-estado` — PR pendiente de abrir (Bloque 1 de `sprint-3-prompts.md`, no ejecutado todavía) |

### Próxima acción

**Bloque 2 y Bloque 3 completos del lado de Claude Code** (checklists preparados, nada ejecutado — ambos requieren la VM de GCP, que solo tiene Rodolfo). Falta la ejecución manual, en este orden:
1. `deployment-gcp.md` §7 (E7-INFRA-03): desplegar `traz-comp-dnato` `develop-v3` en el server de Dnato de ese proyecto (§7.0-bis, comandos concretos), completar la URL real del JWKS, confirmar los puntos de §7.2, aplicar `[[apim.jwt.issuer]]` en el `deployment.toml` de la VM.
2. `deployment-gcp.md` §6 (E7-INFRA-05): desplegar el CAR, publicar API+MCP Server, suscribir la app, verificar OAuth y aislamiento contra `mcp.cloudtrazalog.com`. Los pasos de deploy del CAR y publicación de API/MCP Server (§6.1-§6.3) no dependen de §7 y se pueden hacer antes o en paralelo — solo la verificación real (§6.4-§6.5) necesita la identidad ya aplicada.

En paralelo siguen pendientes: Bloque 1 (PR de relevamiento, no abierto todavía), y las preguntas abiertas de `doc/v3/sprint-3-relevamiento-estado.md`.

**DocTest:** F0, los documentos ancla y **F1 (DNATO) están completos**. **F3 (ALM) pasó el gate**: 16 casos validados y sus `.feature` derivados. Lo próximo es la segunda mitad de F3 —page objects, specs y ayuda de usuario de Almacenes—, que es mecánica y reusa todo el andamiaje de F1. F4 (MCP, Hurl) sigue disponible en paralelo.

**F2 (MAN) espera tres decisiones**: un usuario de AssetPlanner del DEMO (bloquea toda verificación en vivo), si el catálogo de MAN describe `develop` —lo que corre— o `develop-v3` —a donde va el producto—, y qué se hace con los `CP-XX` y la suite E2E que ya existen en el repo de asset. Están planteadas con recomendación en `doctest/catalogo/man/RESUMEN-RELEVAMIENTO-MAN.md`.

**Tres cosas menores esperan una línea del PM** (ninguna frena la segunda mitad): si un Solicitante ve solo sus pedidos o todos los de la empresa (ALM-UC-007, el único caso que quedó en borrador por pedido suyo); si los dos estados de cierre del pedido son correctos; y si está bien que el disponible descuente pedidos todavía sin aprobar.

**Dependencia concreta entre fases:** la empresa de test no tiene el menú de Almacenes —el trigger solo se lo asigna al rol Administrador—, así que para que la suite de ALM corra con datos propios hay que asignárselo desde el ABM de menúes, que es `DNATO-UC-024`, uno de los dos casos de DNATO que quedaron en borrador.

**Lo único que espera decisión del PM en DNATO** (no bloquea ALM): los 2 casos en borrador — DNATO-UC-020 (usuario externo) y DNATO-UC-024 (ABM de menúes). El triaje de hallazgos ya está hecho: 25 de los 35 tienen issue, y los 10 restantes tienen decisión escrita en su fila.


**Agente Minero v3.5 (frente nuevo, rama `develop-v3.5`):** E0 entregado (PR #498, gate 1 resuelto — **Opción C**), el
**Streaming Integrator instalado y verificado** con CDC polling andando contra una tabla real, el **`FirebaseConnectorAPI`
desplegado en el MI local** con el formato de mensaje confirmado por prueba real, y **E1 completo (PR #499)**: esquema
entero probado contra PostgreSQL 16, idempotente, con rollback verificado y **aislamiento multi-tenant 12/12 en verde**.
**Hay dos PRs abiertos — el tope de la metodología —, así que E2 no arranca hasta que se mergee alguno.** Nota de merge:
`STATE.md` va a dar conflicto entre los dos PRs (la rama de E1 salió de `develop-v3.5` limpio y no tiene las filas de E0);
se resuelve conservando las tres filas: AGENTE-E0, AGENTE-SI-01 y AGENTE-E1.

**Lo que espera decisión del PM, en orden de urgencia:**
1. **Dónde vive la base del agente.** El Postgres de DEV (`10.142.0.13`) es 11.18 y no tiene pgvector disponible; el local
   de la máquina de desarrollo es 16.15 y alcanza con `sudo apt install postgresql-16-pgvector`. Sin esto E1 no se puede
   aplicar en ningún ambiente.
2. **Corregir el `faultSequence` vacío del `FirebaseConnectorAPI`** — devuelve 202 aunque el envío falle, y con la Opción C
   eso hace que el Siddhi marque enviado lo que se perdió. Es requisito antes de conectar el agente.
3. **Quién recibe una alerta proactiva de empresa** (bloquea E5; la tabla ya lo contempla con `rol_destino`).
4. Reiniciar el MI local para que registre el connector `googlefirebase`, y un token FCM real para cerrar la prueba de push.
5. Menores: push + campanita o campanita sola; si se rota ahora la clave de Firebase commiteada; y dónde se versiona la app
   Siddhi del agente (se propone `_backend/siddhi/` en este repo).

### Decisiones recientes (últimas 5)

| Fecha | Decisión | Referencia |
|---|---|---|
| 2026-09-02 | **Tres definiciones del PM sobre el puente de alertas.** (1) **Corregir el conector Firebase: sí** — resuelto de forma aditiva con un recurso nuevo `POST /tools/firebase/send-strict` que devuelve 502 si FCM rechaza y 500 si falla la mediación; **`/send` queda intacto** porque cambiarlo dejaría al Siddhi de AssetPlanner reintentando para siempre (filtra por `2\d+` con `on.error=WAIT`). (2) **El destinatario de una alerta proactiva de empresa es un rol**, no un usuario — falta definir cuál y de dónde sale, porque Dnato solo tiene `admin`/`user` mientras que `assetv2.sisgroups` sí tiene roles ricos por empresa (Administrador, Mantenedor, Planificador, Supervisor de Taller). (3) **Canal: push + campanita**, los dos | `doc/agente/analisis-alertas-assetplanner.md` §6.7 y §8, PR #498 |
| 2026-09-02 | **El Streaming Integrator queda como pieza estándar de la arquitectura — decisión del PM sobre el puente de alertas del agente.** Evaluadas tres opciones en el análisis de E0, eligió la C: cola propia en PostgreSQL, app Siddhi nueva y el `FirebaseConnectorAPI` del MI ya existente, sin escribir en `assetv2`. Fundamento: conservar el motor de streaming para cuando las alarmas necesiten reglas, ventanas o correlación —"por futura posible extensibilidad del sistema de alarmas"— y no cargar al orquestador con el transporte, que solo encola. Se instala primero en la máquina de desarrollo; para demo y prod se documenta el despliegue y se arma el flujo ahí | `doc/agente/analisis-alertas-assetplanner.md` §6.1, PR #498 |
| 2026-08-21 | **Una tool MCP puede completar su trabajo y aun así reportar error al cliente: el status HTTP importa tanto como el cuerpo.** El MI responde `202 Accepted` en las tools que escriben; en MCP sobre HTTP el `202` significa "notificación aceptada, sin cuerpo", así que el cliente descarta una respuesta que venía completa y con `isError:false`. El usuario ve error, reintenta, y cada reintento crea un pedido real y un caso de Bonita — 14 pedidos por uno solo. **Lo que hizo caro el diagnóstico: el `api.log` del APIM reporta `RESPONSE_OUT statusCode:200` mientras el cable entrega `202`.** La única fuente confiable de lo que recibe el cliente es el access log del reverse proxy. **Regla operativa: toda validación de tools tiene que verificar el status en el cable y mandar los headers `Mcp-*` como el cliente real** — sin `Mcp-Method` el rewrite de Caddy no matchea y el test da un falso negativo. Se agrega `validar-tools-e2e.py`, que es el único script que recorre el camino completo | `doc/mcp/republicar-mcp-server.md` §7, `deploy/gcp/reverse-proxy/Caddyfile` |
| 2026-08-20 | **Agregar tools a un MCP Server exige recrear la API — no hay camino más corto.** Verificado tras agotar seis alternativas. El síntoma es una tool visible en `tools/list` que falla con `500` (`existingAPIOperationMapping is null`) y una pantalla `Tools` del Publisher que no abre; la causa es que `AM_API_URL_MAPPING` no recibe las filas de las operaciones agregadas después de que el MCP Server fue generado, aunque la definición y las revisiones muestren todo correcto. **Regla operativa: toda tool nueva implica recrear la API y volver a crear el MCP Server desde ella.** Corolario para el diseño: conviene agrupar las tools nuevas en tandas en vez de publicarlas de a una. Se agrega `mcp-smoke-tools.py`, que valida tool por tool sin necesidad de token y distingue mapeo (`401`) de mapping roto (`500`) — es el chequeo que faltaba para no depender del mensaje del conector | `doc/mcp/republicar-mcp-server.md` §7 |
| 2026-08-19 | **DocTest corre sobre el DEMO de v2 y los workflows son autónomos — dos definiciones del PM.** (1) No hay nada de v3 desplegado, así que el sistema bajo prueba es `demo.cloudtrazalog.com` (DNATO `/traz-comp-dnato/main/login`, Tools `/traz-tools`, Asset Planner `/tools/traz-prod-assetplanner`), con la empresa Conservas. Se agregó el project `demo` a Playwright junto a `local` y `staging-v3`, que queda declarado para el día que exista. **Verificado en vivo: login real contra DNATO demo → redirige a `/traz-tools/` con cookie `ci_session` de sesión CodeIgniter** — buena señal para las fixtures con `storageState` del Doc 3 §4.3, que se confirma en F1. Falta una segunda empresa de test para probar aislamiento. (2) Los workflows de DocTest son autónomos: **no** se crean `v3-ci.yml` ni `v3-deploy-staging.yml` (siguen en E7-CICD) y `doctest-full-staging.yml` sale con `workflow_dispatch` interino + TODO a `workflow_run`. Además, del Doc 3 v1.1: el campo `perfil` del catálogo pasa a **vocabulario cerrado** (Solicitante \| Supervisor \| Planificador \| Mantenedor) — el validador rechaza cualquier otro valor a propósito, y los roles de DNATO se agregan al enum recién cuando el relevamiento los identifique y Rodolfo los valide | Doc 2 v1.1, Doc 3 v1.2, `doctest/.env.example`, `doctest/catalogo/catalogo.schema.json` |
| 2026-09-01 | **La carga masiva ya da de alta equipos en AssetPlanner, y eso reabrió una dependencia que no estaba escrita.** El PR #34 de `traz-comp-dnato` agrega despacho por motor: cada entidad declara en `sta.entidades_negocio.motor_bd` contra qué base corre su procedimiento, y las de Mantenimiento (Equipos y Articulos) van a MariaDB. Cierra **H-030 / #470**, que era la entidad ofrecida en pantalla y rota desde el relevamiento. El patrón de MariaDB **no es el de PostgreSQL**: el SP no recibe el archivo, lee de una tabla de staging, así que hay dos pasos donde antes había uno. Actualizados DNATO-UC-026 (0.3→0.4, se cae la nota de "Equipos no anda") y DNATO-UC-027 (0.2→0.3, con dos flujos alternativos nuevos y el despacho en `referencias_codigo`). Lo que aparece de nuevo y queda anotado como **H-061**: la carga contra AssetPlanner traduce la empresa por `core.empresas.empr_id_mysql`, que lo escribe el alta de empresa por el recurso que **#491 tiene roto** — o sea que toda empresa creada mientras #491 estuvo caído no va a poder cargar equipos. **La verificación E2E quedó pendiente: el DEMO no respondía** | PR de DocTest, `doctest/catalogo/dnato/` |
| 2026-08-31 | **`develop-v3` volvió a tener el contenido de `develop`, y al sincronizar apareció un DataService corrupto.** Decisión del PM: tras la v2.5 las dos ramas arrancan iguales y la divergencia empieza de cero. La diferencia eran 15 archivos y en 14 `develop` estaba adelante. `_backend/api/dataservice/MANDataService.dbs` **no contenía el DataService** en `develop-v3` sino un `<api>` de Synapse escrito encima por el commit `6758338c`: la rama había perdido 24 queries y 25 resources de AssetPlanner (KPIs de MTTR/MTTF/disponibilidad, solicitudes de servicio, adjuntos). El merge lo repara. Se resolvió por merge y no por `reset --hard`, para no reescribir 65 commits en una rama protegida. Cierra **H-038 / #481** (cuatro submódulos atrás). **Falta escribir la política de convergencia**: el `CLAUDE.md` sigue diciendo que `develop` no recibe cambios estructurales | PR #495 |
| 2026-08-31 | **v2.5: la capa v3 entró en la línea de v2.** Decisión del PM —"unir cosas de futuro v3 con v2 e ir sincronizando de a poco"—. Entraron el sitio de ayudas, DocTest y ~3.450 líneas de `_backend/`: `toolsMCPAPI`, `toolsALMAPI`, `toolsMANAPI`, `JwtValidator` y las sequences de multi-tenancy. En PHP de runtime cambiaron **solo tres archivos**. El conflicto del `ALMDataService` cortaba en medio de un `<query>` y los dos lados compartían el cierre: juntarlos sin reponerlo dejaba el XML inválido y el DataService no habría levantado. Se conservaron las queries de los dos lados (93 únicas, ninguna perdida). Se retiraron `GET /articulos` y `/articulos/obtener/{arti_id}`, que leen sin filtrar por empresa (#478), verificado que nadie los invoca. **Los cuatro riesgos de despliegue del análisis de #488 siguen sin verificar contra el WSO2 de v2** | PR #494, tag `v2.5` |
| 2026-08-26 | **La guía de DocTest ya explica cómo corre la suite un programador contra su propia máquina.** Nueva `GUIA-PRUEBAS-Y-AYUDAS.md` §1.6, escrita para el programador del equipo que quiere verificar que su cambio no rompió nada antes de pushear: configurar `DOCTEST_ENV=local` con sus tres URLs, qué datos tiene que tener su base (la suite **no** los crea: entra con usuarios que ya existen, y AssetPlanner tiene padrón propio), qué correr según lo que tocó, y un árbol de decisión para la pregunta que importa cuando algo falla — *¿es mi cambio o es el test?*. Dos puntos que la sección deja explícitos: **verde no es "no rompí nada" sino "no rompí nada de lo cubierto"** —hay módulos sin un solo caso—, y **un spec que molesta no se edita**, porque es derivado del YAML; se pide `/doctest`. Queda dicho también que el entorno `local` está soportado por la configuración pero **nadie lo ejecutó todavía** contra un Apache local: toda la suite corrió siempre contra el DEMO | `doctest/GUIA-PRUEBAS-Y-AYUDAS.md` §1.6 |
| 2026-08-26 | **F2 (MAN) entregada: catálogo validado, manual y suite en verde.** 37 casos —27 validados, 9 en borrador, 1 obsoleto—, el manual de Mantenimiento con 10 secciones y **16 tests E2E**. La suite completa de los tres módulos: **71 tests en verde** (11,5 min). **La pieza que destrabó todo fue el BPMN**, descifrado en `doc/man/proceso-mantenimiento.md`: todo el mantenimiento es un solo proceso de Bonita, el trabajo llega a la bandeja de entrada en vez de buscarse en el menú, **los estados los mueve el proceso y nadie los elige**, y una sola decisión —¿es urgente?— parte el circuito en dos, lo que además explica qué es el backlog: la solicitud que podía esperar. Los estados quedaron traducidos leyendo qué escribe cada tarea al cerrarse (`S`, `PL`, `C`, `T`/`TE`, `CE`, `CN`), y el equipo tiene tres (`AC`, `IN`, `AN`) con **baja lógica**: `baja_equipo()` no borra el registro. Se descubrió qué es la meta del equipo —`meta_disponibilidad`, que compara el Panel de KPIs— y que **el máximo y el mínimo de un parámetro no disparan nada**: son referencia para la persona. **MAN necesitó su propia fixture de sesión** porque tiene ingreso y padrón propios. Sigue abierto #489: ningún usuario creado por la registración puede entrar a AssetPlanner | PR #487, `doctest/catalogo/man/`, `doc/man/proceso-mantenimiento.md` |
| 2026-08-25 | **F2 (MAN/AssetPlanner) arrancada — y AssetPlanner no comparte la sesión de Tools, lo que frena la verificación en vivo.** 6 casos del piloto (alta de equipos y componentes) en `borrador`. **Ya había trabajo previo valioso en `develop-v3` del repo de asset** que no conviene duplicar: `circuitos-man-alm-pan.md` (310 líneas, con verificación en base), `casos-prueba-man.md` (catálogo CP-01…) y una suite E2E de 6 specs. De ahí sale el mapa: **un solo motor de OT** (`Calendario::guardar_agregar`), **el pedido de materiales es perezoso** —nace al abrir "Ejecutar OT", no del plan— y **materiales y herramientas son asimétricos**: los materiales tienen circuito transaccional con descuento de stock, las herramientas solo se declaran. **Bloqueante**: AssetPlanner tiene su propio padrón (`sisusers`) y las credenciales de Tools devuelven error — hace falta un usuario del DEMO (H-048), y MAN va a necesitar su propia fixture de sesión. **Nuevo hallazgo**: quedaron +15 llamadas `dump()` activas que imprimen HTML en la respuesta, una de ellas dentro de `guardar_componente()`, justo el alta del piloto (#486). Sobre versiones, al revés que en traz-tools: acá **`develop-v3` está 34 commits adelante** y contiene todo `develop`; el alta de equipos **no difiere entre las dos**, así que el piloto no depende de eso | PR de F2, `doctest/catalogo/man/`, `doc/hallazgos/REGISTRO.md` |
| 2026-08-25 | **Catálogo de ALM validado — y al profundizar donde el PM lo pidió, cinco respuestas resultaron distintas de lo relevado.** 19 casos → **16 validados, 2 en borrador, 1 obsoleto** (editar un pedido: hoy no se puede), y 16 `.feature` derivados. Lo que cambió al estudiar mejor el código: **el disponible del punto de pedido descuenta también los pedidos sin aprobar** —el filtro excluye estados cerrados, así que uno en `Creada` ya reserva—; **el pedido tiene ocho estados, no cuatro** (aparecen `Finalizado Ent. Parcial` y `Finalizado Sin Entrega`, que los cambia Bonita); el precio del stock valorizado sale del que se carga **al recibir**, y se activa por empresa con un registro en `core.tablas` cuya `tabla` contiene `alm_configs` (campo `valor2`); las reglas de "no entregar de más" y "no sacar más de lo que hay" **existen pero solo en la pantalla** (#483); y **mi relevamiento del ajuste de stock estaba mal** —había capturado el modal de detalle en vez del alta, y buscado en el PHP una lógica que vive en el DataService (`REST_ALM /stock/ajuste/detalle_batch_req`)—, lo marcó el PM y se rehizo. Nuevo: el código de edición de pedidos sigue expuesto aunque la pantalla no lo use (#484). Verificado que el relevamiento sí fue contra lo desplegado: releví `de535f6`, el mismo commit que apunta `develop`. **Son cuatro los submódulos** que `develop-v3` tiene atrás, no uno (#481). El método para leer `develop` sin tocar `develop-v3` quedó escrito en la guía §1.4 | PR #482, `doctest/catalogo/alm/`, `doctest/GUIA-PRUEBAS-Y-AYUDAS.md` |
| 2026-08-25 | **F3 (ALM) relevada y en gate de validación — el relevamiento encontró que una empresa lee los datos de otra.** 19 casos de Almacenes en `borrador` con 61 preguntas, sobre las 13 pantallas del menú verificadas una por una contra el DEMO. **Verificado en vivo con dos sesiones simultáneas**: desde DocTest Empresa SA se obtuvo el pedido 882 de Conservas completo y el artículo 1477 (#478). La causa no es puntual: los listados filtran por empresa pero **las 36 funciones que buscan por id no**, así que el aislamiento vive en la pantalla y no en los datos (#479, con tres opciones de alcance) — **es el mismo gap que ADR-012 detectó en el DataService, del lado del PHP**, y toca al piloto MCP porque `get_pedido_material` debe venir vacío para otra empresa. Además: `empr_id` fijo en 1 en los pedidos extraordinarios (#480) y **el submódulo `traz-comp-almacenes` de develop-v3 está 25 commits atrás de lo desplegado** (#481) — el menú tiene Movimientos Internos, la pantalla funciona en el DEMO y su controlador no está en el árbol de v3, así que el relevamiento se hizo contra `origin/develop` del submódulo. Vocabulario: se propone el perfil **`Responsable de Almacén`**, que sale de los roles que crea `toolsCOREAPI` — verificado que su nombre **sí** coincide con el del trigger de menúes, no es el caso de H-012. Ninguna operación de escritura se ejecutó contra el DEMO: tiene datos de una empresa real | PR de F3, `doctest/catalogo/alm/`, `doc/hallazgos/REGISTRO.md` |
| 2026-08-25 | **Las ayudas se publican desde el propio repo y el acceso ya está en la barra de todas las pantallas.** Decisión del PM: las ayudas son **parte de traz-tools**, no un sitio aparte. El generador escribe en **`ayuda/` en la raíz del repo, versionada**: el `.htaccess` sirve tal cual lo que existe en disco, así que se ve en `<base_url>ayuda/` sin tocar CodeIgniter, y como el frontend no tiene build step el deploy no necesita Node — publicar una corrección de la ayuda es un commit más, revisado por PR. El acceso (ícono `?` en la barra, pestaña nueva) ya lo había agregado el equipo en `develop` apuntando a `trazalog.com/ayudatools`; en `develop-v3` apunta a la carpeta propia. **Ojo en el sync semanal de v2 → v3: esa línea va a conflictuar y se resuelve con la versión de v3**, anotado en un comentario en la propia vista. El sitio viejo sigue sirviendo a v2 hasta el cutover. **Triaje de hallazgos hecho**: 8 pasaron a issue (#470 a #477 — carga masiva de Equipos contra MariaDB, carga masiva sin verificar perfil, los 4 roles de SMA sin menú, no se pueden corregir los datos de la empresa, cambio de contraseña sin pedir la actual, vencimiento de enlaces por fecha, "contactá soporte" sin decir cómo, y un mensaje de error copiado de otra pantalla) y 10 quedan en el registro porque ya tienen decisión escrita del PM | PR #469, `ayuda/`, `application/views/layout/perfil.php`, `doc/hallazgos/REGISTRO.md` |
| 2026-08-25 | **F1 cerrada: DNATO queda con catálogo validado, tests, Gherkin y ayuda de usuario — y la suite encontró seis bugs que la lectura de código no había visto.** 28 casos (25 validados, 2 en borrador por decisión pendiente, 1 obsoleto), 25 `.feature`, **55 tests E2E en verde** (smoke de 6 flujos en 1m37, completa 7,3 min) y el manual `manual_registracion_y_cuenta.html` armado con la plantilla extraída del sitio real —tuteo, `theme.css` compartido, anclas `sNN`— con el buscador del inicio regenerado sobre **las 26 secciones de todos los manuales**, que era la deuda del RF-05.3. **Bugs que encontraron los tests**: recuperar la contraseña no cambia nada porque `updatePassword()` escribe en `users` sin el esquema `seg.` (#467 🔴), editar un usuario no existe —el controlador no carga ninguna vista— (#468 🔴), el alta de usuarios termina en un callejón sin salida (#464 🔴), la pantalla Perfil llega vacía (#463), el reset termina en página en blanco (#466). **Y cuatro hallazgos míos quedaron desmentidos** al verificarlos contra la pantalla (H-017, H-018, H-019, H-020): la vista ya protegía lo que el controlador parecía dejar abierto. La lección quedó escrita en el registro. Decisiones de arnés: ingreso una sola vez en setup global, suite en serie (el DEMO es chico), el caso de cierre de sesión con su propia sesión, y el alta completa de empresa como test aparte (`@alta-empresa`) porque crea datos reales | PR #450 (catálogo validado) + PR #469 (tests, `.feature` y ayuda), `doc/hallazgos/REGISTRO.md` |
| 2026-08-25 | **La suite de DNATO llegó a 31 tests en verde, encontró dos bugs más y desmintió tres hallazgos míos.** Cubiertos con test los casos de registro (incluido el camino feliz, que **lee el mail de activación solo**), ingreso, salida, perfil, editar perfil, lista de usuarios con aislamiento, alta de usuario, habilitar/inhabilitar y el corte de acceso a la administración de empresas. Smoke recortado a los 6 flujos críticos: 1m37 (RNF-02 pide < 2 min); suite completa 5 min. **Bugs nuevos que encontró la suite**: la pantalla Perfil devuelve una página en blanco (#463) y —el más importante— **el alta de usuarios termina en un callejón sin salida** (#464): la pantalla dice "asignale roles desde la Lista de Usuarios" y el usuario recién creado **no aparece ahí**, porque esa consulta hace INNER JOIN contra las membresías. **Tres hallazgos míos quedaron desmentidos al verificarlos contra la pantalla** (H-017, H-018, H-019): la vista ya protegía lo que el controlador parecía dejar abierto; los tres issues quedaron corregidos y bajados de severidad, y la lección quedó anotada en el registro — **un hallazgo de interfaz no se reporta sin verificarlo contra la pantalla real** | `doctest/tests/e2e/specs/dnato/`, `doc/hallazgos/REGISTRO.md` |
| 2026-08-25 | **Primeros tests automatizados de DocTest corriendo en verde — y el primero encontró un bug que ninguna lectura de código había visto.** Con los 25 casos validados se derivaron los 25 `.feature` (generador `catalog-to-feature.ts`, transformación mecánica, no se editan a mano) y los primeros 4 specs: ingreso, cierre de sesión, perfil y lista de usuarios con **aislamiento entre empresas verificado end-to-end**. 12 tests, tres corridas seguidas en verde, smoke en 1m53 (RNF-02 pide < 2 min). **Hallazgo nuevo del propio test (H-031, issue #463): la pantalla Perfil devuelve una página en blanco** porque `profile.php` llama a `image()` y el helper define `imageAdmin()` — error fatal de PHP silenciado. Convención adoptada para los casos que describen el comportamiento esperado y todavía fallan: `test.fail()` con el issue anotado, así el test sigue vivo y **la corrida se pone en rojo el día que el bug se corrija**, avisando que hay que sacar la marca. Tres decisiones de arnés que salieron de correrlo de verdad: el ingreso se hace una sola vez en un setup global (dentro de un test se comía el tiempo máximo), la suite corre **en serie** porque el entorno compartido no aguanta paralelismo, y el caso de cierre de sesión **no puede usar la sesión compartida** porque `sess_destroy()` la invalida para toda la suite | `doctest/tests/e2e/`, `doctest/features/dnato/` |
| 2026-08-24 | **Catálogo de DNATO validado por el PM: 22 validados, 5 en borrador, 1 obsoleto — y 9 bugs confirmados sobre reglas de negocio que el sistema no cumple.** La validación se hizo sobre una hoja legible generada del propio catálogo (`npm run hoja:validacion`), no sobre los YAML. Reglas que el PM fijó y el código no cumple, cada una con issue: la recuperación de contraseña es solo para quien validó su correo (#455), el usuario no puede cambiar su propio correo (#456), editar un usuario no debe exigir contraseña nueva (#457), el alta de empresa del superusuario debe validar y configurar igual que la registración (#458), un administrador no puede inhabilitarse a sí mismo (#459), el alta de usuario solo ofrece las empresas donde es administrador (#460), y los menúes solo se asignan a roles de la propia empresa (#461). Definición fuerte: **nunca debe existir un borrado definitivo de usuarios, porque rompe la trazabilidad** (#451). Se relevó además el resto del menú —carga masiva y baja de empresa— y ahí apareció un bug 🔴 nuevo: **"Eliminar empresa" no borra la empresa, borra el usuario cuyo id coincida con el id de empresa** (#462). El registro de hallazgos quedó en 29 entradas. **Lo validado describe el comportamiento esperado, no el actual**: los derivados de esos casos van a fallar hasta que se corrijan los bugs, que es lo que tiene que pasar en una suite de regresión | `doctest/catalogo/dnato/`, `doc/hallazgos/REGISTRO.md` |
| 2026-08-24 | **La registración de DNATO quedó automatizable de punta a punta: el test se crea su propia casilla de correo.** El enlace de activación llega por mail, así que sin leer el correo el caso no se puede probar solo. Gmail ya no admite IMAP con contraseña común y la cuenta de prueba no puede emitir contraseña de aplicación, así que se resolvió por otro camino: `tests/e2e/fixtures/casilla-descartable.ts` crea una casilla temporal por API, el seed se registra con ella y lee el mail solo. **Cero credenciales que guardar** (ni en `.env` ni en Secrets), bandeja vacía en cada corrida y mismo comportamiento en local y en CI; el límite anotado es que el dominio es público, así que sirve para pruebas y nunca para producción. **Verificado creando una segunda empresa real sin intervención humana** (`DocTest Empresa 2608242245`, usuarios en `@emalupe.com`). Con las dos empresas de test ya se pudo **verificar el aislamiento con datos reales**: cada administrador ve sus seis usuarios y el superadmin, ninguno ve los de la otra empresa — que es lo que prueba DNATO-UC-013 | `doctest/tests/e2e/fixtures/casilla-descartable.ts` |
| 2026-08-24 | **Empresa de test creada recorriendo el alta real, no por SQL: `DocTest Empresa SA`.** El script `npm run seed:empresa` hace los cinco pasos del registro freemium (registro → activación → información adicional → alta de empresa → bienvenida) y deja la empresa con su administrador (`admindoctest@gmail.com`, perfil Admin), sus 16 roles, establecimiento, depósito y los cinco usuarios iniciales en `@doctest-empresa.com`. Verificado después: entra al sistema, aparece una sola vez en el combo del login y su menú **no** incluye Gestión de Empresas, o sea que sirve como administrador no-superusuario para probar aislamiento (DNATO-UC-013, que estaba bloqueado). Correcciones que salieron de correrlo de verdad: el combo de países trae la bandera en el texto, el teléfono debe cumplir el patrón argentino `+54 9XXXXXXXXXX`, y el submit del alta de empresa es `Guardar y Continuar` (no un botón genérico). **Sigue bloqueada la automatización de la registración**: Google no acepta la contraseña normal por IMAP y hace falta una contraseña de aplicación para que el test lea el mail de activación solo | `doctest/tests/e2e/seeds/crear-empresa-test.ts` |
| 2026-08-24 | **Los hallazgos pasan a tener un lugar único: `doc/hallazgos/REGISTRO.md`.** Pedido del PM. Es el registro central de bugs, deudas y mejoras que aparecen mientras se hace otra cosa y no se corrigen en esa tarea; cada fila lleva evidencia (archivo y línea) y estado, y **los bugs además abren issue** con label `hallazgo`. Arranca con los 14 del relevamiento de DNATO (H-001 a H-014), cuatro de ellos con issue: **#451** borrado físico de usuarios, **#452** el rol *Responsable de Procesos* nunca ve su menú, **#453** `edituser`/`deleteuser` sin verificación de pertenencia, **#454** usuarios iniciales con contraseña `12345` visible. **Hallazgos nuevos de esta pasada, del trigger `core.configuracion_inicial_empresa_trg()`**: los menúes de una empresa nueva se asignan solos (confirmado por el PM), pero el trigger cubre **8 de los 16 roles** —Mantenimiento y SMA quedan afuera— y uno de esos ocho está escrito sin el "de" (`Responsable Procesos`), así que apunta a un rol que no existe. El trigger además habilita al superadmin en cada empresa nueva y crea las listas de valores por defecto | `doc/hallazgos/REGISTRO.md` |
| 2026-08-19 | **DocTest F1 (2da pasada): los permisos de DNATO son DOS sistemas distintos, y el relevamiento inicial los había confundido.** (1) **Perfil de DNATO** (`seg.users.role` → `seg.roles`): dice qué puede ADMINISTRAR — `Admin` habilita usuarios, menúes, carga masiva y configuración; `Author` solo los datos propios; `Usuario externo` (rol 8) representa a una empresa de afuera. Por encima, el **superusuario** no es un perfil de la base sino **un correo hardcodeado en `constants.php` (`TOOLS_ADMIN_USER`)**, único que ve Gestión de Empresas. (2) **Roles de trabajo de Tools**: viven en Bonita por empresa y dicen qué puede HACER; **los crea el alta de empresa en `toolsCOREAPI` POST /empresa — 16 roles con el nombre `<Rol> <NombreEmpresa>`** (Almacén, Producción, Lote, Pañol, Tareas, Procesos, los 4 de Mantenimiento y los 4 SMA), mapeados como actores de los procesos. `seg.memberships_menues` ("Menu por Rol") conecta esos roles con lo que cada usuario ve en el menú. **Decisiones del PM**: ABM de Empresas, Menúes y Configuración entran en la Ola 1 con su perfil correcto; la automatización del registro va contra una empresa descartable (con procedimiento de limpieza como deuda anotada); la empresa de test se crea de cero porque Conservas tiene datos incompletos (**verificado: tiene 1 solo rol de trabajo**); reCAPTCHA no se usa; y **v3 elimina el desplegable de empresas del login** — se validan credenciales y recién ahí se ofrece selector si el usuario tiene más de una. **Hallazgo 🔴 nuevo: el borrado de usuarios es FÍSICO** (`DELETE` en `seg.users_business` y `seg.users`), no lógico como se asumía, y no da de baja en Bonita ni en Asset | `doctest/catalogo/dnato/RESUMEN-RELEVAMIENTO-DNATO.md` §1 y §5 |
| 2026-08-19 | **DocTest F1: DNATO relevado — 21 casos en `borrador`, ninguno derivado, y 8 hallazgos que documento sin corregir.** Cubre registración freemium (registro → activación → formulario → alta de empresa → bienvenida), acceso (login con selección de empresa, salir, recuperar contraseña, login OAuth de agentes), cuenta propia y administración de usuarios de la empresa. Fuera de alcance por ahora, pendiente de tu decisión: ABM de Empresas, Menús y Carga Masiva. **Tres decisiones bloquean la derivación**: (a) los tres perfiles nuevos que propone el relevamiento (`Administrador`, `Usuario`, `Visitante`) que el Doc 1 v1.1 RF-05.4 anticipa pero no nombra, (b) hasta dónde se automatiza el registro, que **crea empresa + 5 usuarios BPM + establecimiento + depósito sin baja automática**, y (c) que hace falta una **segunda empresa de test** para poder probar que un administrador no ve usuarios de otra empresa. Hallazgos: los 5 usuarios iniciales nacen con contraseña `12345` mostrada en la pantalla de bienvenida; editar un usuario obliga a reescribir su contraseña; `edituser`/`deleteuser` no verifican pertenencia a la empresa del administrador; el combo del login lista todas las empresas del sistema sin sesión | `doctest/catalogo/dnato/RESUMEN-RELEVAMIENTO-DNATO.md` |
| 2026-08-19 | **DocTest F0: infraestructura levantada, con 4 decisiones técnicas menores tomadas por CC y una desalineación de documentos detectada.** (1) El proyecto Node vive en `doctest/package.json`, no en la raíz — la raíz es PHP/CodeIgniter y no tenía `package.json`; así el tooling de tests no contamina el frontend. (2) Los scripts npm pasan por dos envoltorios Node (`scripts/pw.mjs`, `scripts/test-module.mjs`) porque Playwright resuelve `--project` antes de leer el `.env`, y `VAR=x comando` no es portable a Windows. (3) El CI de validación corre `playwright test --list` en vez de `test:smoke`: carga config y specs sin navegador ni credenciales, y no se rompe cuando F1 agregue specs reales — la ejecución real vive en `doctest-e2e.yml` (F5). (4) Se agregó la regla **R6** al validador (cambio de comportamiento de un caso `validado` sin bump de `version`), que el Doc 3 §3.2 pide pero no exigía en CI; corre solo con `--diff-base`. **Desalineación reportada y confirmada por el PM:** el Doc 1 del repo estaba desactualizado; el RF-05.2/RF-05.3 que citaba el prompt existen en el **Doc 1 v1.1**. Los 3 documentos vigentes (Doc 1 v1.1, Doc 2 v1.1, Doc 3 v1.2) entran por su propio PR (#446, DOCTEST-DOCS) y salen de esta rama para no duplicarse | `doctest/README.md`, `doctest/catalogo/SCHEMA.md`, issues #437-#442 |
| 2026-08-15 | **Los KPIs de mantenimiento miden sobre la fuente equivocada — verificado en producción.** Disponibilidad, MTTR, MTBF y cantidad de fallas se calculan sobre `historial_lecturas`, tomando como "falla" cada lectura en estado `RE`. En Tierra Capayán (empresa 15) hay **4 lecturas `RE` en dos años y medio contra 17.372 solicitudes de reparación**: el mantenimiento se registra, pero por el circuito de solicitudes/OTs, que los KPIs no miran. Corregir la fórmula no alcanza — depurando tramos negativos y arreglando la unidad, el MTBF da 8.115.231 horas (926 años), porque el denominador sigue siendo 4. **Los KPIs hay que reconstruirlos desde las órdenes de trabajo**, y para eso falta una decisión funcional que Claude Code no toma: qué campo de la OT marca el inicio y el fin de una parada, y cómo se distingue una falla de un preventivo (el flag `correctivo` está NULL en las 17.372). **Consecuencia para v3: bloquea exponer los KPIs como tools MCP** — expondrían números que no representan la operación. Hallazgo separable y corregible ya: el cierre de tramos ordena por `id_lectura` en vez de por fecha e invierte el 22,6% de los tramos. Corrección de un diagnóstico previo mío: llegué a decir que el 99,74% de disponibilidad era "casualidad aritmética"; es falso, esa query **sí** filtra los tramos invertidos (8 veces en su WHERE) y su aritmética es correcta | `doc/mantenimiento/kpis-consultas-a-ingenieria.md`, `scripts/dev/kpi-*.sql` |
| 2026-08-12 | **REQ-ASSET-ALM: asset consume el ALM y el PAN de tools YA, sin migración masiva de datos — validado contra producción.** Requerimiento del PM que adelanta la Etapa 5 del plan de migración y la simplifica: conteos read-only en `assetv2` productivo muestran el almacén operativo de asset en **cero absoluto** (pedidos, remitos, órdenes de insumos, lotes, envíos) y catálogos con uso mínimo — único cliente real con datos es Caleras San Juan (32 artículos, 2 herramientas → carga manual en setup, no migración). Decisiones acordadas (D1-D10, registradas en el CONTEXT-PACK de asset): depósito y pañol únicos por convención con ids resueltos en setup, puente de empresas por lookup REST (falta crear `getEmpresaByMysqlId` — la registración freemium escribe el vínculo solo en `core.empresas.empr_id_mysql` y asset no guarda nada de tools), DataServices como bus interno sin validación por usuario para acceso por id (MCP sigue sin usarlos), mismo Bonita para ambos, menús inhabilitados no borrados, y **excepción explícita al freeze**. Hallazgo bloqueante: **la copia EI de `ALMDataService.dbs` (la que despliega tools hoy) tiene `getArticulos2` con `WHERE empr_id = 1` HARDCODEADO y sin el fix de aislamiento del 2026-08-11** — la sincronización EI←MI pasa de mejora pendiente a prerrequisito (F1). Metodología de tools adaptada al repo de asset: `develop-v3` creada desde `develop`, CLAUDE.md + CONTEXT-PACK + STATE propios, un PR por fase | asset PR #322, `traz-prod-assetplanner/doc/v3/CONTEXT-PACK.md` |
| 2026-08-12 | **La migración de AssetPlanner se retoma, y el punto de partida es el fork de 2024 — pero hay que sanearlo antes de sumar una línea.** Cuatro decisiones del PM: (1) rescatar el fork de Rogelio Sanchez y re-mergear encima la divergencia, en vez de re-migrar desde cero — se justifica porque de los 43 archivos portados sólo 15 cambiaron en 20 meses; (2) alcance = núcleo de mantenimiento primero, y **almacenes y herramientas después** — en ambos casos asset tiene la versión vieja, así que no se migran sino que se **reemplazan** por `traz-comp-almacenes`/`ALMDataService` y `traz-comp-pan` (pañol)/`PANDataservice`, con **migración de los datos de los clientes a PostgreSQL en el cutover**; (3) acceso a datos directo por `$db['asset_db']` a MariaDB como **excepción acotada** a la convención de `CLAUDE.md`, con plan de salida a DataServices (requiere ADR-014); (4) freeze de features en assetplanner durante la migración, sólo bugfixes con cherry-pick a ambos lados. El relevamiento corrigió dos supuestos: el trabajo **nunca se integró** (el submódulo apunta a "First commit", el código vive en una rama que ningún repo referencia) y el módulo **no arranca** — la conversión quedó a medias por método en vez de por archivo. Bloqueante identificado: no existe staging del frontend PHP, sólo del stack WSO2. **El cutover queda en dos velocidades**: el núcleo corre sobre la misma MariaDB (sin migración de datos, rollback instantáneo), mientras que almacenes y herramientas mueven datos a PostgreSQL y su rollback exige restore. Punto duro a resolver antes de migrar: ocho tablas de asociación (~150 referencias) se quedan en MariaDB apuntando a entidades que pasan a PostgreSQL — mismo problema que ADR-009 resolvió para empresas con `empr_id`/`empr_id_mysql` | `doc/migracion/assetplanner-a-traz-tools-man.md` |
| 2026-08-11 | **Tool nueva `alm_get_depositos` + suite de escenarios encadenados (7/7 OK con escrituras reales).** Probando el flujo real apareció un hueco que ninguna prueba de tools sueltas podía encontrar: `alm_crear_pedido_materiales` exige un `depo_id` por artículo y **ninguna tool devolvía los depósitos** — el usuario quedaba trabado justo antes de crear el pedido. Implementada en las 3 capas + declarada en la OpenAPI (con la instrucción explícita al agente de consultarla en vez de preguntarle el ID al usuario). Nueva suite `scripts/dev/mcp_escenarios.py`: 7 escenarios que encadenan tools pasando el resultado de una a la siguiente como haría el agente (reposición por punto de pedido, equipo en falla → OT, auditoría de OTs, seguimiento de pedidos, aislamiento entre 2 empresas, coherencia listado/detalle, y cross-check OpenAPI ↔ implementación). Verificado end-to-end **incluidas las escrituras**: creó el pedido 1483 y la OT 293 con instancia real en Bonita (`case_id` 30006) | `doc/mcp/escenarios-de-uso-y-regresion.md` |
| 2026-08-11 | **✅ `alm_get_stock` devolviendo datos reales desde Claude, end-to-end contra GCP.** Barra de acero 50.0, Barra de acero cortada 307.0, Eje 51.0 — coincide con lo que muestra la app v2. Cierra el circuito completo: OAuth 2.1 PKCE (Dnato) → APIM (`mcp.cloudtrazalog.com`) → MI → `toolsMCPAPI` → `toolsALMAPI` → DataService → PostgreSQL. **Corrección de un diagnóstico previo mío:** llegué a afirmar que el CAR pisaba el datasource y lo dejaba apuntando a desarrollo (`10.142.0.13`); Rodolfo objetó que esa IP no es alcanzable desde la VM de GCP y tenía razón — el datasource apuntaba correctamente a `10.142.0.11`. Lo di por probado sin verificarlo en la VM. La causa real del vacío fue que la copia del repo en el servidor de demo no estaba actualizada. Lección incorporada al doc: verificar el datasource **desempaquetando el `.car` desplegado**, y comparar el nombre de la base además del host (en `10.142.0.11:5434` conviven varias bases con esquema equivalente) | `doc/mcp/verificacion-queries-datos-reales.md` §1 |
| 2026-08-11 | **4 bugs de queries encontrados y corregidos verificando las 9 tools contra datos reales.** (1) 🔴 `alm_get_stock` sumaba stock de otras empresas — `getArticulos2` unía `alm_lotes` sin filtrar por empresa: la empresa 87 reportaba 5982 cuando 3001 eran de la empresa 1. (2) `man_get_equipos` ocultaba equipos con catálogos incompletos por usar `INNER JOIN` a los 6 catálogos descriptivos: 2 equipos de la empresa 8, ambos en estado `RE`, no aparecían. (3) `man_get_ots` perdía y duplicaba a la vez (INNER JOIN a equipos descartaba solicitudes con `id_equipo=-1`; el join a `orden_trabajo` duplicaba porque el modelo admite N OT por `case_id`). (4) `man_get_ot` duplicaba por lo mismo. Verificación final: **23/23 completitud y 19/19 aislamiento en verde**. Scripts reutilizables en `scripts/dev/verify-mcp-{queries,isolation}.py` (extraen el SQL de los `.dbs`, sirven para CI) | `doc/mcp/verificacion-queries-datos-reales.md`, `ALMDataService.dbs`, `MANDataService.dbs`, `MANEquiposDataService.dbs` |
| 2026-08-11 | **✅ MCP Server de GCP funcionando end-to-end desde Claude.** `alm_get_stock` → `{"materias":{}}` y `man_get_equipos` → `{"equipos":{}}`, ambas HTTP 200 con la estructura correcta. La cadena completa quedó verificada: OAuth 2.1 PKCE contra Dnato → APIM gateway (`mcp.cloudtrazalog.com`) → MI → `toolsMCPAPI` → `toolsALMAPI` → DataService → PostgreSQL. **Lo que falta es data, no integración**: no hay stock cargado para la empresa de prueba y `empr_id_mysql` sigue en NULL. Último gotcha resuelto: el `endpointConfig` del MCP Server no se puede editar desde la UI del Publisher en 4.6.0 — hay que hacerlo por REST API y desplegar revisión (§6.3-ter). Y varios "fallos" reportados eran en realidad el MI arrancando (~40 s tras el redeploy del CAR) | `doc/v3/deployment-gcp.md` §6.3-bis/§6.3-ter, DoD de E7-INFRA-05 |
| 2026-08-11 | **Bug 🔴 de aislamiento encontrado y corregido: las 4 tools `alm_*` pasaban `jwt_mysql_empresa_id` (id de `assetv2`/MySQL) a consultas que corren contra PostgreSQL.** `ALMDataService` usa `ToolsDataSource` (Postgres) y filtra por `alm.alm_lotes.empr_id` = el id de `core.empresas` (ADR-012 §52-54). Copy-paste del patrón de las tools `man_*`, que sí deben usar el id de MySQL. **No se detectó en el smoke test de DEV porque la empresa de prueba tenía `empr_id = empr_id_mysql = 1`** — el bug es invisible cuando ambos ids coinciden. Apareció en GCP con `empr_id=9000` y `empr_id_mysql` en NULL. Riesgo que cierra: con `empr_id_mysql` poblado con un valor que exista como `empr_id` de otra empresa en Postgres, las tools `alm_*` habrían devuelto **datos de otra empresa en silencio**. Corregido a `jwt_empr_id` en las 4, con el comentario explicativo que faltaba. Aprobado por Rodolfo. Build verificado | `toolsMCPAPI.xml`, PR de esta rama |
| 2026-08-11 | **OAuth discovery resuelto en GCP — la cadena RFC 9728 → 8414 → 7591 responde en verde.** Ejecutados los 4 gaps de APIM (CAR de discovery, system properties, `https_endpoint`, `[[apim.jwt.issuer]]` ya estaba). Gotchas nuevos documentados: `carbonapps/` no existe en una instalación limpia del APIM (hay que crearlo, sí viene de fábrica en el MI); arrancar el APIM con `api-manager.sh` en vez de systemd deja el proceso **sin** las system properties (el PRM sale con los campos vacíos — buen síntoma de diagnóstico); y un proceso huérfano corriendo como root bloquea el arranque por systemd (`PID file is not owned by root. Refusing`). Del lado de Dnato, el bug #4 de §7.0-quater (rutas `oauth/*`) seguía presente: se había dado por corregido verificando solo el JWKS, sin probar la AS metadata ni `/oauth/register` | `doc/v3/deployment-gcp.md` §6.2-bis/§6.2-ter |
| 2026-08-11 | **Descubierta una SEGUNDA instancia de WSO2 que nadie había documentado: el backend de Dnato en `localhost:8283` (VM `vm-demo-trazalog`), distinta del MI de la fachada MCP (`8290`, VM `mcp-trazalog`).** El login OAuth de Dnato no pasa por la fachada MCP — llama a su propio backend. Actualizar el CAR en la VM del MCP no actualiza ese otro, que quedó viejo y no expone `/empresa/{empr_id}` (existe en `develop-v3`: `COREDataService.dbs:303` y `:820`). Además **falta la columna `empr_id_mysql` en `core.empresas`, y no existe ninguna migración SQL para ella en NINGÚN repo** — se asume en toda la cadena de ADR-009 pero nunca se formalizó. Bloqueante para PROD, candidato a issue propio. Nueva §7.0-quinquies | `doc/v3/deployment-gcp.md` §7.0-quinquies |
| 2026-08-11 | **Confirmado que los grupos de Bonita SÍ usan el formato `{empr_id}-{nombre}`** (`"name":"9000-Termotanques"` en el log del login real) — cierra la duda que había quedado abierta en §7.0-quater. `Oauthlogin.php` (que espera ese formato) está bien; el bug es de `Cli.php`, que aplica el mismo parseo contra `seg.memberships_users.group`, donde el prefijo no existe | `doc/v3/deployment-gcp.md` §7.0-quater |
| 2026-08-10 | **Gap crítico encontrado en el checklist de E7-INFRA-05: faltaba TODO el bloque de OAuth discovery del APIM.** Al intentar dar de alta el conector en Claude.ai contra la VM de GCP, el flujo OAuth fallaba con un 404 en `https://mcp.cloudtrazalog.com/authorize`. Causa raíz: `deployment-gcp.md` §6 documentaba solo el `.car` del **MI** (`ToolsAPIProject`) y omitía por completo el **segundo CAR, el del APIM** (`trazalog-discovery-1.0.0.car`, de `_backend/api/ApimDiscoveryProject`), que sirve el PRM RFC 9728 y es el que le dice a Claude que el Authorization Server es Dnato. En DEV nunca se notó porque `scripts/dev/setup-ngrok.sh` lo despliega solo (paso 3b) junto con las system properties, el `https_endpoint` y el issuer del Key Manager — todo automático, y por eso nunca se documentó como paso manual. 4 gaps de APIM (CAR ausente, `JAVA_OPTS` sin las system properties, `https_endpoint` en `localhost:8243`, Key Manager anunciándose a sí mismo como AS en violación de TAD-IDENT-04). Del lado de Dnato **no faltaba nada**: G2/G3 de `oauth-discovery-flow.md` ya están implementados en `develop-v3` (`Oauth.php::register_client()`, `routes.php:66`). Nueva §6.2-bis (procedimiento) y §6.2-ter (troubleshooting de los síntomas reales en Claude); `install-apim.sh`, `wso2am.service` y `.env.example` actualizados para que una VM nueva lo haga solo | `doc/v3/deployment-gcp.md` §6.2-bis/§6.2-ter, `deploy/gcp/` |
| 2026-08-09 | **§7 de `deployment-gcp.md` reescrita con troubleshooting real completo**, ejecutando el checklist de identidad de punta a punta contra `demo.cloudtrazalog.com`. Encontrados y resueltos en vivo: cadena TLS incompleta (intermedio Sectigo mal configurado en Apache + raíz faltante en el trust store del server, viejo/CentOS 7 EOL — troubleshooting completo en §7.0-ter), y 6 bugs de `traz-comp-dnato` (§7.0-quater): `composer_autoload` en `FALSE`, `permitted_uri_chars` sin `@`, sintaxis `??` (PHP7+) en `Cli.php` corriendo sobre PHP 5.6, rutas de `oauth/*` faltantes en `routes.php`, ruta relativa en `JWT_PUBLIC_KEY_PATH` del `.htaccess`, y resolución de `empr_id` en `Cli.php` por parseo de prefijo en vez de lookup a `core.empresas` (confirmado por Rodolfo como bug real, no solo de este server). Los 4 bugs de código (no el de `empr_id`, que es de lógica/datos) quedan marcados como candidatos a PR en `traz-comp-dnato` — si PROD se despliega con `git clone` en vez de copiar archivos a mano, se pisan contra los mismos | `doc/v3/deployment-gcp.md` §7.0-ter, §7.0-quater |
| 2026-08-08 | **Corrección en §6.1/§6.2 de `deployment-gcp.md`: usar `mvn clean install` (Maven del sistema), no `./mvnw`.** Rodolfo, ejecutando el checklist en la VM Rocky Linux, encontró "comando desconocido" al correr `./mvnw`. Causa raíz: `mvnw`/`mvnw.cmd`/`.mvn/wrapper/maven-wrapper.jar` están en `.gitignore` de `ToolsAPIProject` — nunca se commitearon, así que un `git clone`/`git pull` fresco (como el de la VM) nunca los trae. La verificación "BUILD SUCCESS" documentada en §6.1 se había hecho contra un `./mvnw` local que no viene de git. Confirmado que Maven del sistema (3.8.7) coincide con lo que pide `maven-wrapper.properties` | `doc/v3/deployment-gcp.md` §6.1-§6.2 |
| 2026-08-08 | **PR #421 (E7-INFRA-03) se había mergeado contra la rama equivocada — reconciliado acá.** Lo abrí con base `feature/e7-infra-05-deploy-mcp-facade-gcp` (PR #420) en vez de `develop-v3`, para que el diff quedara limpio. Cuando #420 se mergeó primero (squash) y recién después se aprobó/mergeó #421, el squash de #421 quedó aplicado sobre esa rama feature, no sobre `develop-v3` — §7 nunca llegó a integración. "Squash and merge" siempre mergea contra el base branch que tenía el PR al momento de mergear, no contra `develop-v3` por default. Reconciliado con un merge manual (`git merge origin/feature/e7-infra-05-deploy-mcp-facade-gcp` sobre `develop-v3` actualizado) en este PR | `doc/v3/STATE.md`, `doc/v3/deployment-gcp.md` |
| 2026-08-08 | **Corrección en §7: SÍ hace falta desplegar `traz-comp-dnato` en el servidor de Dnato de GCP.** Rodolfo repreguntó tras ver commits de identidad en GitHub que la sección anterior decía que no hacían falta. Verificado directamente en `traz-comp-dnato` (clon local): los commits de JWT/OAuth 2.1 PKCE/JWKS (E9-IDENT-03/04) están SOLO en `develop-v3` de ese repo, no en `develop` ni `master` — si el servidor de Dnato de ese proyecto GCP corre otra rama, no tiene ese código. Agregado §7.0-bis con el checklist real (deploy de `develop-v3`, generar claves RS256 en ese server, migración DB) — trabajo de `traz-comp-dnato`, solo señalado acá como dependencia | `doc/v3/deployment-gcp.md` §7.0-bis |
| 2026-08-08 | **Tarea 3.5 completada (checklist, no ejecución — requiere acceso a la VM que solo tiene Rodolfo).** `deployment-gcp.md` §6 nueva: despliegue del CAR, publicación de API+MCP Server, verificación OAuth y aislamiento contra `mcp.cloudtrazalog.com`. Incorpora todos los gotchas reales encontrados el mismo día en el smoke test de DEV para que no se repitan en GCP. Flagea explícitamente la dependencia con E7-INFRA-03 (identidad) para el paso de aislamiento | `doc/v3/deployment-gcp.md` §6 |
| 2026-08-08 | **Smoke test real del MCP Server (:8243) completado, a pedido de Rodolfo.** 9/9 tools OK, aislamiento OK, escrituras con Bonita real OK. Encontrados y corregidos en vivo 2 bugs de configuración que bloqueaban todo (suscripción faltante de la app `TrazalogDnatoMCP` al MCP Server nuevo, y endpoint mal configurado en API y MCP Server — apuntaba a `10.142.0.13:8280`, dato incorrecto mío desde la Tarea 3.4, corregido a `localhost:8290`). Documentado como paso nuevo (§2.8-bis) y troubleshooting en `virtual-mcp-unificado.md` para que no se repita | `doc/mcp/virtual-mcp-unificado.md` |
| 2026-08-08 | **Paths reales del MCP Server confirmados**: Rodolfo publicó `Trazalog MCP Server`, contexto `/trazalog/mcp`, versión `1.0` — no `trazalog-operaciones`/`/trazalog-operaciones` como sugería ADR-013. Resuelve la pregunta abierta de nombre del ADR. Docs y spec actualizados; tests Hurl no necesitaron cambios (apuntan al MI, capa sin tocar) | `doc/mcp/virtual-mcp-unificado.md`, `doc/api/trazalog-operaciones.yaml`, `doc/adr/ADR-013-unificacion-mcp.md` |
| 2026-08-08 | **Corrección en `virtual-mcp-unificado.md`**: la sección de seguridad OAuth2 (§2.4) decía que había que seleccionar "Dnato" como Key Manager en el Publisher y des-seleccionar Resident KM — instrucción incorrecta escrita en la Tarea 3.4, sin cruzar contra el doc canónico de identidad. Bloqueó a Rodolfo al intentar publicar `trazalog-operaciones` (esa opción no existe: Dnato se valida vía `[[apim.jwt.issuer]]`, no como Key Manager). Corregido con el procedimiento real de `apim-keymanager-dnato.md` §5: no tocar el selector de Key Managers, desactivar `Enable Subscription Validation` | `doc/mcp/virtual-mcp-unificado.md`, `doc/identity/apim-keymanager-dnato.md` |
| 2026-08-08 | **Prueba de humo de E2-MCP-13 en DEV, a pedido de Rodolfo.** 9/9 tools verificadas con datos reales (empr_id=1) contra el MI local con VPN+Postgres arriba. Encontrado y arreglado (Rodolfo pidió el fix directo: "el arreglo del case_id es simple, lo podrias ejecutar por favor") un bug preexistente en `MANDataService.dbs`: `case_id` se leía del LEFT JOIN a `orden_trabajo` en vez de la tabla base `solicitud_reparacion`, dando `null` en OTs recién creadas | `fix/e2-mcp-case-id-null-man-queries`, `MANDataService.dbs` |
| 2026-08-08 | **Doc de Ambientes creado**, a pedido de Rodolfo, tras confundirse con un redirect a ngrok al entrar a Publisher. Documenta cuándo DEV necesita ngrok (testing MCP con Claude.ai, federación Dnato) y cuándo no (trabajo diario), más el estado y acceso de TEST/PROD. Se identificó y corrigió la causa raíz del problema puntual: `hostname` en el `deployment.toml` local quedó pisado con un subdominio ngrok muerto por no haber corrido `--reset` al cerrar sesiones anteriores | `doc/infra/ambientes.md`, `scripts/dev/setup-ngrok.sh` |
| 2026-08-03 | **Tarea 3.4 completada.** OpenAPI unificada de las 9 tools + doc de Virtual MCP Server único con guía de migración coordinada. Detectada y corregida staleness en `openapi-publish-procedure.md` (describe `X-Empr-Id` en vez del `X-JWT-Assertion` vigente de ADR-009) — no corregida en el doc viejo, solo evitada en el nuevo | `doc/api/trazalog-operaciones.yaml`, `doc/mcp/virtual-mcp-unificado.md` |
| 2026-08-02 | Rodolfo confirmó el nombre real del proceso Bonita para `alm_crear_pedido_materiales`: **"Pedido de Recursos Materiales"** — la etiqueta `"Ped. Materiales"` de `constants.php` (usada como suposición inicial) NO era el nombre real registrado en Bonita. Corregido en `toolsALMAPI.xml` y en el test Hurl | `toolsALMAPI.xml`, `tests/security/mcp-facade-alm-tools.hurl` |
| 2026-07-31 | **Tarea 3.3 (almacenes) completada y mergeada.** `toolsALMAPI.xml` (nuevo, contexto `/tools/alm`) como capa de orquestación que recibe `empr_id` EXPLÍCITO (no derivado de JWT) — decisión de Rodolfo, reusable a futuro por el PHP de v2. `toolsMCPAPI` expone 4 tools `alm_*` como fachada delgada. 3 queries aditivas en `ALMDataService.dbs`. Rollback de 3 niveles | PR #413, `toolsALMAPI.xml`, `toolsMCPAPI.xml`, `ALMDataService.dbs` |
| 2026-07-31 | E2-MCP-12 completada y mergeada: las 5 tools de mantenimiento en `toolsMCPAPI` con prefijo `man_`, ninguna reimplementa lógica | PR #411 |
| 2026-07-31 | E2-MCP-11 mergeado (PR #410): ruteo múltiple a distintos DataServices desde una sola API MI confirmado | PR #410 |

### Bloqueos

- ~~La máquina de desarrollo no tiene espacio para instalar el Streaming Integrator~~ **Resuelto 2026-09-02** — se liberaron 2 GB (cache de Chrome, thumbnails, cache de npm, papelera) y el SI quedó instalado. La raíz sigue ajustada (3,2 GB libres de 117 GB); quedan pendientes de ejecutar por el PM dos comandos con sudo que liberan varios GB más: `journalctl --vacuum-size=200M` (~2,1 GB) y la limpieza de revisiones viejas de snap (23 GB en `/snap`).
- **Clave privada del service account de Firebase commiteada en `traz-tools` (🔴, derivado de AGENTE-E0).** `_backend/api/FirebaseConnectorAPI/.../FirebaseConnectorAPI.xml` y sus copias `.backup`/`tmp` tienen la `privateKey` completa de `firebase-adminsdk-3ag87@traz-prod-assetplanner.iam.gserviceaccount.com` en claro; permite enviar push a cualquier dispositivo del proyecto. Rotar en la consola de Firebase y mover a la config del MI. Espera decisión del PM (pregunta 4 del gate 1).
- **`MANDataService.getNotificaciones` filtra solo por `user_id`, sin `empr_id` (🟠, derivado de AGENTE-E0).** Contradice el patrón de ADR-009. Hoy solo lo llama el PHP de AssetPlanner con el id de sesión, pero el endpoint no valida nada por sí mismo. El puente del agente NO lo va a reutilizar; corregirlo para AssetPlanner es decisión aparte.
- **El push de AssetPlanner no está verificado end-to-end en ningún ambiente accesible (🟠, derivado de AGENTE-E0).** En la base de desarrollo hay 434 filas encoladas y 0 procesadas desde 2024-03; los `.siddhi` de `traz-int` apuntan a bases distintas de la de DEV; y los formatos de mensaje del Siddhi de dev, el de producción y `MessageCreateSeq` del MI no coinciden entre sí. Antes de construir el puente conviene un `POST` real contra `/tools/firebase/send` con un token de prueba.
- ~~OAuth discovery del APIM~~ **Resuelto 2026-08-11** — la cadena de discovery responde en verde (§6.2-bis).
- **Los datasources viven embebidos en el `.car`, con credenciales en texto plano en el repo.** Cada instancia necesita su propia copia del repo con las URLs de su ambiente, y un checkout desactualizado en el servidor produce un CAR con la config equivocada (pasó el 2026-08-11 en demo). **Mejora pendiente de decisión (🔴)**: moverlos a `[[datasource]]` del `deployment.toml` del MI — config por ambiente y sin secretos en el artefacto. Ver `doc/mcp/verificacion-queries-datos-reales.md` §1.
- **Mojibake en 3 equipos de `assetv2` (🟡, corrección de DATOS no de config).** `equipos.descripcion` es una columna latin1 con bytes UTF-8 adentro en 3 filas (BOMB-001, GHOR-001, BOMB-SJ-001 — las cargadas en el smoke test de Sprint 2); al pedirlas como UTF-8, MySQL las double-encodea. **No se arregla cambiando el datasource**: de las 8 filas con acentos, 5 están en latin1 correcto y 3 en UTF-8, así que cualquier charset rompe una mitad. Requiere normalizar esas filas puntuales. Ver `escenarios-de-uso-y-regresion.md` §5.1.
- **16 queries de `ALMDataService` que usa el PHP de v2 tienen el mismo bug de stock cross-empresa** que se corrigió en `getArticulos2` (join a `alm_lotes` sin filtro de empresa). No se tocaron por estar fuera del alcance MCP y no poder probarse sin la app v2 — pero si el stock que muestra v2 sale de ahí, tiene el mismo sesgo. Ver §6 de ese mismo doc.
- **`empr_id_mysql` no tiene migración SQL en ningún repo** — la columna se asume existente en `core.empresas` (usada por `COREDataService.getEmpresaById`/`updateEmpresaAssetId` y por toda la cadena de identidad de ADR-009), pero nunca se formalizó como migración. Faltaba en la base de TEST y hubo que agregarla a mano. **Bloqueante para el despliegue a PROD** si no se crea la migración antes. Ver §7.0-quinquies.
- **El WSO2 backend de Dnato (`vm-demo-trazalog:8283`) está desactualizado** respecto de `develop-v3` — no expone `/empresa/{empr_id}`. Es una instancia distinta del MI de la fachada MCP; redesplegar el CAR ahí (no parchear el `.dbs` a mano, se pierde en el próximo deploy). Ver §7.0-quinquies.
- **El checkout de `traz-comp-dnato` en el server de demo está desincronizado de `develop-v3`** — van 3 archivos con código faltante que sí existe en el repo (`routes.php`, `Oauthlogin.php`, `Empresas.php`). Conviene sincronizar el checkout completo en vez de seguir parcheando de a uno. Ver §7.0-quater.
- **Antes de ejecutar los pasos de consola de `virtual-mcp-unificado.md`:** confirmar que `apim.jwt.enable=true` está activo en el `deployment.toml` del APIM (debería estarlo ya, si `trazalog-equipos`/`trazalog-ots` funcionan hoy) — sin eso, el backend JWT `X-JWT-Assertion` no se genera y `EmprIdFromHeader` falla con `identity_missing` para toda la API nueva.
- **Confirmado (2026-08-08, smoke test real): WSO2 4.6.0 NO lee `x-mcp-annotations` del OpenAPI** — `readOnlyHint`/`destructiveHint` no aparecen en el `inputSchema` de `tools/list`. Gap preexistente de Sprint 2 (`equipos.yaml`/`ot.yaml` tampoco la usaban), no introducido por esta tarea, no bloqueante — Claude igual sabe cuándo pedir confirmación por la descripción semántica de cada tool.
- Config de identidad (ADR-008/009) y migración de DataServices a PostgreSQL siguen pendientes antes de poder dar de alta al primer cliente sobre la VM de GCP — cubierto por E7-INFRA-03 (Bloque 2, no ejecutado todavía).
- **Nota operativa (de E2-MCP-11, sigue vigente):** el deploy del `.car` de `ToolsAPIProject` es atómico — si `ALMDataService` (o cualquier otro `.dbs`) no puede conectar a su BD al momento del deploy, se revierte el CAR completo.
- La implementación de escritura MCP en Almacenes sigue bloqueada hasta que el PM confirme cómo resolver el aislamiento multi-tenant de `ALMDataService` (ver pregunta abierta #1 del relevamiento de Sprint 3) — posible tema de arquitectura (🔴).
