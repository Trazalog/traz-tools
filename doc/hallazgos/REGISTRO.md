# Registro de hallazgos

## Objetivo

Es el lugar único donde se anotan los **hallazgos** del proyecto: bugs, deudas y mejoras que aparecen mientras se hace otra cosa (un relevamiento, una migración, un despliegue) y que **no se corrigen en esa misma tarea**. Está escrito para el PM, que tría y prioriza, y para Claude Code, que lo alimenta. **No** es un backlog de features (eso son los issues del Project) ni un registro de decisiones (eso es `doc/v3/STATE.md`).

Sin este archivo, un hallazgo vive en la descripción de un PR que se mergea y se pierde de vista. Acá queda con su evidencia y su estado.

---

## Cómo se usa

**Al encontrar algo:**

1. Agregar una fila a la tabla con el próximo `H-NNN`. Nunca se reutiliza un número.
2. Escribir la evidencia concreta: archivo y línea, o la consulta/verificación que lo demuestra. Un hallazgo sin evidencia no entra.
3. **Si es un bug** (el sistema hace algo distinto de lo que dice hacer): además se abre un issue de GitHub con label `hallazgo` + `bug`, y se pone su número en la columna Issue.
4. **Si es una mejora o una deuda**: queda solo acá hasta que el PM decida. Si decide hacerlo, ahí se abre el issue.

**Regla:** el hallazgo se documenta, **no se corrige en la tarea que lo encontró** — salvo que el PM lo pida explícitamente. Corregir de paso mezcla el diff y rompe la revisión.

**Estados:** `abierto` · `en issue #N` · `corregido en #N` · `descartado (motivo)` · `es correcto` (se verificó y funciona como se espera).

**Severidad:** 🔴 datos incorrectos, fuga entre empresas o pérdida de información · 🟡 afecta al usuario pero tiene rodeo · 🟢 cosmético o interno.

> **Lección de método (2026-08-25).** Dos hallazgos de esta tanda —H-017 y H-019— nacieron de leer el controlador sin mirar la vista, y los dos estaban mal: la pantalla ya protegía lo que el controlador parecía dejar abierto. **Un hallazgo de interfaz no se reporta sin verificarlo contra la pantalla real**, y si ya se reportó, se corrige el issue en vez de dejarlo. Lo que sí queda en esos casos es la defensa en profundidad que falta del lado del servidor, que es un hallazgo distinto y de severidad menor.

---

## Triaje del 2026-08-25

De los 18 hallazgos que no tenían issue, **8 pasaron a issue** (#470 a #477) y **10 quedan solo acá**.

El criterio: entra a issue lo que tiene una acción clara y no espera ninguna definición —los bugs, y
las dos mejoras que dejan al usuario sin salida (no poder corregir los datos de la empresa, y los
errores que piden contactar a soporte sin decir cómo)—. Quedan en el registro los que **ya tienen
decisión tomada del PM** escrita en su propia fila: "aceptado por ahora" (H-003), "lo resuelve el
cambio de login de v3" (H-004, H-005), "se deja como está" (H-015) y las mejoras marcadas como
futuras (H-006, H-021, H-022, H-025, H-026, H-027). Esos no necesitan issue: necesitan que alguien
los priorice algún día, y para eso está esta tabla.

---

## Hallazgos

| ID | Fecha | Origen | Repo / Módulo | Tipo | Sev | Resumen | Evidencia | Estado |
|---|---|---|---|---|---|---|---|---|
| H-001 | 2026-08-19 | DocTest F1 | traz-comp-dnato / registración | bug | 🔴 | Los cinco usuarios iniciales de cada empresa nacen con la contraseña `12345`, y la pantalla de bienvenida la muestra en pantalla. Nada obliga a cambiarla | `constants.php:230`, `Register::registro_completo()` | en issue #454 |
| H-002 | 2026-08-19 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🔴 | "Eliminar usuario" hace **borrado físico**: `DELETE` sobre `seg.users_business` y después sobre `seg.users`. Se esperaba baja lógica. Además no da de baja al usuario en Bonita ni en Asset, así que queda huérfano ahí. **Decisión del PM (2026-08-24): nunca debe existir un borrado definitivo, porque rompe la trazabilidad de lo que ese usuario hizo. La baja tiene que ser lógica** | `User_model.php:770-795`, `Main.php:693-741` | en issue #451 |
| H-003 | 2026-08-19 | DocTest F1 | traz-comp-dnato + BD | deuda | 🟡 | El superusuario está definido **en dos lugares distintos y desacoplados**: `TOOLS_ADMIN_USER` en `constants.php` (controla el menú de Gestión de Empresas) y `core.tablas.coresuper_admin` (lo usa el trigger de alta de empresa, con `jperez@prueba.com` como default hardcodeado). Si no coinciden, el sistema tiene dos "superusuarios" con poderes distintos. **El trigger además agrega ese usuario a TODA empresa nueva con rol Administrador** — verificado en vivo el 2026-08-24: aparece en la lista de usuarios de la empresa de test recién creada. **El PM lo acepta por ahora**; queda como posible mejora | `constants.php:105`, `configuracion_inicial_empresa_trg_func.sql:15-25,55-60` | abierto — aceptado por ahora |
| H-004 | 2026-08-19 | DocTest F1 | traz-comp-dnato / login | mejora | 🟡 | El combo de empresas del login lista **todas** las empresas del sistema a cualquiera que abra la pantalla, sin sesión | `Main.php:1362-1400` | abierto — lo resuelve el cambio de login previsto en v3 |
| H-005 | 2026-08-19 | DocTest F1 | traz-comp-dnato / login | mejora | 🟢 | Los mensajes de login y recuperación distinguen entre "no existe el correo", "no pertenece a la empresa" y "cuenta no aprobada": permiten averiguar desde afuera si un correo está registrado | `Main.php:1412`, `Main.php:1505-1512` | abierto |
| H-006 | 2026-08-19 | DocTest F1 | traz-comp-dnato | mejora | 🟢 | Código muerto: `Main::associaterol()` carga una vista `membership` que no existe en el repo; `changelevel.php`, `changeleveluser_old.php`, `changeuser_old`, `banuser_old` y `Bulkload copy.php` son versiones anteriores sin enlace en el menú. **El PM declaró obsoleta la pantalla vieja de habilitar/inhabilitar (2026-08-24)** | `Main.php:1012-1030`, `application/views/` | abierto |
| H-007 | 2026-08-19 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🟡 | Editar un usuario **obliga a reescribir su contraseña**: `edituser()` marca contraseña y confirmación como `required`, igual que el alta. Cambiar solo el apellido fuerza una contraseña nueva. **Subsumido por H-035 (2026-08-25): la pantalla de edición ni siquiera existe**, así que estas reglas nunca llegan a mostrarse | `Main.php:742-771` | en issue #457, supeditado a #468 |
| H-008 | 2026-08-19 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🔴 | `edituser()` y `deleteuser()` **no verifican** que el usuario objetivo pertenezca a una empresa del administrador conectado: con el id en la URL, un administrador puede editar o borrar usuarios de otra empresa | `Main.php:693-771` | en issue #453 |
| H-009 | 2026-08-19 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🟢 | El mensaje de error de habilitar/inhabilitar dice "Error al borrar usuario" — texto copiado de otra pantalla | `Main.php:500-554` | en issue #477 |
| H-010 | 2026-08-19 | DocTest F1 | traz-comp-dnato / cuenta | bug | 🟡 | Cambiar la contraseña propia no pide la contraseña actual y, a diferencia de la activación, **no se propaga** a Bonita ni a Asset: lo que sí falta es pedir la contraseña actual antes de cambiarla. **Acotado por el PM (2026-08-24): que no se propague a Bonita ni a Asset NO es un problema — la contraseña la administra Dnato y nadie más**; queda solo la mejora de seguridad | `Main.php:555-610` | en issue #474 |
| H-011 | 2026-08-19 | DocTest F1 | traz-comp-dnato / tokens | bug | 🟢 | El vencimiento de los enlaces de activación y recuperación compara la **fecha** de creación con la de hoy (`createdTS != todayTS`): valen hasta la medianoche, y si la columna guardara hora ningún enlace validaría nunca | `User_model.php:111-135` | en issue #475 |
| H-012 | 2026-08-24 | DocTest F1 | traz-tools + BD / alta de empresa | bug | 🔴 | **Un rol se queda sin menú por una diferencia de nombre**: el alta crea el rol `Responsable de Procesos <empresa>` (en `toolsCOREAPI`) pero el trigger le asigna el menú a `Responsable Procesos <empresa>` (sin "de"). Los dos nombres nunca coinciden, así que ese rol no ve el módulo Procesos | `toolsCOREAPI.xml` (roles del POST /empresa) vs `configuracion_inicial_empresa_trg_func.sql:33-44` | en issue #452 |
| H-013 | 2026-08-24 | DocTest F1 | traz-tools + BD / alta de empresa | bug | 🟡 | El alta de empresa crea **16 roles** pero el trigger asigna menúes a **8**. Quedan sin ningún menú los cuatro de Mantenimiento (Supervisor, Planificador, Solicitante, Mantenedor) y los cuatro de SMA/Residuos (Transportista, Generador, Operario Descarga, Operador de Bascula). Para Mantenimiento puede ser correcto (vive en Asset Planner, aplicación aparte), pero **SMA es un módulo de Tools**. **Acotado por el PM (2026-08-24): que Mantenimiento no reciba menú es correcto —todavía vive en Asset Planner y no está migrado—, así que el hallazgo queda reducido a los cuatro roles de SMA/Residuos** | `toolsCOREAPI.xml` vs `configuracion_inicial_empresa_trg_func.sql:33-44` | en issue #472 — acotado a SMA |
| H-014 | 2026-08-19 | DocTest F1 | traz-comp-dnato / empresas | deuda | 🟡 | El alta de empresa del superusuario **no valida duplicados** (el control de razón social está comentado) y **no crea** el establecimiento, el depósito ni los usuarios iniciales, a diferencia del alta por registración. Dos caminos para lo mismo, con resultados distintos. **Confirmado como bug por el PM (2026-08-24): los dos altas tienen que validar lo mismo y dejar la empresa igual de configurada** | `Empresa.php:46-105` (control comentado en 118-121) vs `Register::guardarEmpresa()` | en issue #458 |
| H-015 | 2026-08-24 | DocTest F1 | traz-comp-dnato + BD / perfiles | mejora | 🟢 | **Los perfiles de DNATO no se llaman igual en los datos que en el negocio**: `seg.roles` guarda `Admin` y `Author` (herencia de la plantilla original del sistema, junto con `Editor` y `Subscriber` que no se usan), mientras que funcionalmente son *Administrador* y *Usuario*. El usuario ve "Author" en la columna Nivel de Usuario sin que eso signifique nada para él. **Decisión del PM (2026-08-24): se deja como está por ahora**; el catálogo y las ayudas usan el lenguaje de negocio (`Administrador`, `Usuario`) y el renombre de los datos queda como mejora futura | `seg.roles`, `Userlevel.php`, `changeleveluser.php` (campo Perfil) | abierto — futura mejora |
| H-016 | 2026-08-24 | DocTest F1 | traz-comp-dnato / cuenta | bug | 🔴 | **Cualquier usuario puede recuperar la contraseña, aunque nunca haya validado su correo.** La pantalla solo exige que la cuenta esté `approved`, y tanto los cinco usuarios iniciales de cada empresa como los que crea el administrador nacen ya en `approved`. Regla del PM (2026-08-24): la recuperación es solo para quien tenga el correo validado — el administrador que dio de alta la empresa | `Main.php:1505-1512`, `Register.php:1042`, `User_model.php:957` | en issue #455 |
| H-017 | 2026-08-24 | DocTest F1 | traz-comp-dnato / cuenta | bug | 🟢 | **Corregido el 2026-08-25 al verificarlo en la app**: la pantalla **sí** protege el correo (`views/changeuser.php` lo declara `readOnly`), así que por la interfaz no se puede cambiar. Lo que queda es que **el servidor confía en ese readonly**: `updateuser()` guarda el correo que llegue en el POST, y como es la identidad del usuario en Bonita y en `users_business`, un pedido armado a mano desalinea la cuenta. Defensa en profundidad faltante, no un agujero alcanzable desde la pantalla | `views/changeuser.php` (readonly) vs `Main.php:611-672` | en issue #456 |
| H-018 | 2026-08-24 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🟢 | **Corregido el 2026-08-25 al verificarlo con un test**: la pantalla **no** ofrece al propio administrador, así que por la interfaz no puede inhabilitarse. Queda que `banuser()` no compara el correo del POST contra el de la sesión — misma defensa en profundidad faltante que #456 y #460 | `views/banuser.php:8-19` vs `Main.php:500-554` | en issue #459 |
| H-019 | 2026-08-24 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🟢 | **Corregido el 2026-08-25 al verificarlo con un test**: el desplegable **sí** ofrece solo las empresas del administrador (`views/adduser.php:77` usa `emp_connect`, no `obtenerBusines()`). Lo que queda es que el servidor no revalida la empresa que llega en el POST, y que `dd_business` se calcula y viaja a la vista sin que nadie la use | `views/adduser.php:77` vs `Main.php:259` | en issue #460 |
| H-020 | 2026-08-24 | DocTest F1 | traz-comp-dnato / menúes | bug | 🟢 | **Corregido el 2026-08-25 al verificarlo con un test**: el desplegable **sí** ofrece solo las empresas del administrador (`views/menu/menurolesList.php:160` recorre `emp_connect`). Queda que `addMenuRoles()` no revalida el grupo que llega en el POST — misma defensa en profundidad faltante que #456, #459 y #460 | `views/menu/menurolesList.php:160` vs `Menu.php:98-157` | en issue #461 |
| H-021 | 2026-08-24 | DocTest F1 | traz-comp-dnato / registración | mejora | 🟢 | No hay depuración de cuentas registradas que nunca confirmaron el correo: quedan en Dnato sin activar y ocupan la dirección para siempre | `Main::procesarRegistro()` | abierto — mejora futura |
| H-022 | 2026-08-24 | DocTest F1 | traz-comp-dnato / registración | mejora | 🟢 | Las respuestas del formulario de información adicional (cómo se enteró, rubro, tamaño, problemas) se guardan y **no se usan para nada**. Idea del PM: una tool MCP para el superusuario que explote esos datos | `Register::guardarFormularioRegistro()` | abierto — mejora futura |
| H-023 | 2026-08-24 | DocTest F1 | traz-comp-dnato / empresas | mejora | 🟡 | **No hay dónde corregir los datos de la empresa después del alta.** El PM decidió no validar el identificador tributario, lo que hace imprescindible poder editarlo si se cargó mal — y hoy no existe esa pantalla | `Empresa.php` (solo listar, agregar y borrar) | en issue #473 |
| H-024 | 2026-08-24 | DocTest F1 | traz-comp-dnato / empresas | mejora | 🟢 | Los mensajes de error del alta piden "contactá soporte" sin decir cómo: deberían mostrar `soporte@trazalog.com` | `Register.php` (mensajes de `guardarEmpresa`) | en issue #476 |
| H-025 | 2026-08-24 | DocTest F1 | traz-comp-dnato / usuarios | mejora | 🟢 | La etiqueta "Rol" del alta de usuario es en realidad el **perfil de Dnato** (Admin/Author), no un rol de trabajo: se confunde con los roles de Tools | `application/views/adduser.php` | abierto |
| H-026 | 2026-08-24 | DocTest F1 | traz-comp-dnato / configuración | mejora | 🟡 | La pantalla de Configuración del sitio (título, zona horaria, reCAPTCHA, tema) **queda fuera de alcance y probablemente haya que ocultarla**: es global del sistema y la puede tocar cualquier administrador de cualquier empresa | `Main::settings()` | abierto — caso DNATO-UC-021 marcado obsoleto |
| H-027 | 2026-08-24 | DocTest F1 | traz-comp-dnato / menúes | mejora | 🟢 | El ABM de opciones de menú es muy rústico (sin validaciones declaradas, campos técnicos expuestos). Candidato a rehacerse, sin prioridad | `Menu.php`, `views/menu/menuesList.php` | abierto — mejora futura |
| H-028 | 2026-08-24 | DocTest F1 | traz-comp-dnato / empresas | bug | 🔴 | **"Eliminar empresa" no borra la empresa: borra un usuario.** `Empresa::borrarEmpresa($empr_id)` es una copia del borrado de usuarios — busca un usuario con el id de la empresa, usa `$busines` sin definir y termina en `deleteUser($empr_id)`, un `DELETE` físico sobre `seg.users` por id. Si algún usuario tiene ese id, se borra el usuario equivocado y la empresa sigue existiendo. El modelo ya tiene `Empresas::eliminarEmpresa()`, que es la correcta, y la pantalla no la usa | `Empresa.php:132-176` | en issue #462 |
| H-029 | 2026-08-24 | DocTest F1 | traz-comp-dnato / carga masiva | bug | 🟡 | **La carga masiva no verifica el perfil**: el controlador solo exige sesión iniciada (`if (!$this->session->userdata('email'))`). El menú la muestra solo a administradores, pero cualquier usuario autenticado llega por dirección directa y puede cargar datos | `Bulkload.php:18-48` | en issue #471 |
| H-030 | 2026-08-24 | DocTest F1 | traz-comp-dnato / carga masiva | bug | 🟡 | **La carga masiva de Equipos falla**: el procedimiento no funciona contra MariaDB, que es donde viven los equipos (Asset Planner). Las otras entidades —Artículos, Herramientas y Stock de Artículos— sí cargan. **El PM lo quiere resuelto en el corto plazo** | `Bulkload::procesarCarga()` + procedimientos `sta.sp_carga_masiva_*` | **corregido** — el PR #34 de `traz-comp-dnato` (2026-08-31) agrega el despacho por motor: cada entidad declara en `sta.entidades_negocio.motor_bd` contra que base corre. Cierra #470 |
| H-031 | 2026-08-24 | DocTest F1 | traz-comp-dnato / cuenta | bug | 🟡 | **La pantalla "Perfil" queda en blanco.** `views/profile.php:8` llama a `image()`, pero el helper define `imageAdmin()`: es un *call to undefined function*, error fatal, y la respuesta llega vacía (200 con `<html><head></head><body></body></html>`). El usuario ve una página en blanco sin ningún mensaje. **Lo encontró el primer test automatizado de la suite**, no una lectura de código | `views/profile.php:8` vs `helpers/image_helper.php:6` | en issue #463 |
| H-032 | 2026-08-25 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🔴 | **El alta de usuarios termina en un callejón sin salida**: la pantalla avisa que hay que asignarle roles desde la Lista de Usuarios, pero el usuario recién creado **no aparece ahí**, porque `getListUserDataForGroups()` hace INNER JOIN contra `memberships_users` y él todavía no tiene ninguna. Solo se lo ve después de asignarle un rol, que es lo que no se puede hacer. Verificado: sí figura en "Habilitar/Deshabilitar Usuario", que usa otra consulta | `User_model.php:730-732` | en issue #464 |
| H-033 | 2026-08-25 | DocTest F1 | traz-comp-dnato / cuenta | bug | 🟡 | **Restablecer la contraseña termina en una página en blanco.** La contraseña sí cambia, pero `reset_password()` redirige a `checkLoginUser()`, que está escrito para alguien que ya inició sesión: usa `$data['email']`, `first_name` y `last_name` —que en este flujo no existen— para mandar el correo de "nuevo inicio de sesión". El usuario nunca ve "Tu contraseña se actualizó correctamente" ni vuelve al ingreso | `Main.php:1587-1629` → `Main.php:64-115` | en issue #466 |
| H-034 | 2026-08-25 | DocTest F1 | traz-comp-dnato / cuenta | bug | 🔴 | **Recuperar la contraseña no cambia la contraseña.** `updatePassword()` es la única consulta del modelo que escribe en `users` **sin el esquema `seg.`**, así que el UPDATE no toca la fila del usuario. Verificado con el ciclo completo: después de "cambiarla", la nueva da credenciales incorrectas y la vieja sigue funcionando. Es el único camino de recuperación que tiene un usuario, y no lleva a ningún lado — agravado por #466, que además le oculta cualquier señal de error | `User_model.php:379` | en issue #467 |
| H-035 | 2026-08-25 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🔴 | **Editar un usuario no funciona: la pantalla no existe.** `Main::edituser($id)` declara seis reglas de validación, arma el título y termina **sin cargar ninguna vista**, y en `views/` no hay ningún `edituser`. La dirección devuelve una página vacía de 39 bytes. Sin esto, un usuario mal cargado solo se puede "arreglar" dándolo de baja —que además borra físicamente (#451)— y creándolo de nuevo | `Main.php:742-771`, `application/views/` | en issue #468 |
| H-036 | 2026-08-25 | DocTest F3 | traz-comp-almacenes / pedidos | bug | 🔴 | **El detalle de un pedido de materiales se lee desde cualquier empresa.** `getNotaPedidoIds()` arma el `WHERE` solo con `pema_id`. **Verificado en vivo**: con la sesión de DocTest Empresa SA se obtuvo el pedido 882 de Conservas completo — artículo "Durazno descarozado", cantidad 72, código PP001. ADR-012 planifica exponer esto como tool MCP `get_pedido_material` especificando que debe venir vacío si es de otra empresa: conviene arreglar el filtro antes de exponer la tool | `models/Notapedidos.php::getNotaPedidoIds()`, `controllers/Notapedido.php::getNotaPedidoId()` | en issue #478 |
| H-037 | 2026-08-25 | DocTest F3 | traz-comp-almacenes | bug | 🔴 | **El aislamiento entre empresas de ALM vive en la pantalla, no en los datos.** Los listados filtran por `empr_id`, pero **36 funciones de modelo que buscan o modifican por id no lo hacen**: alcanza con cambiar el número para leer o tocar lo de otra empresa. **Verificado en vivo dos veces** desde la sesión de otra empresa: el pedido 882 (H-036) y `Articulo/getLotes/1477`, que devolvió "Artículo: Durazno descarozado" de Conservas. Las de escritura (`update_editar`, `editarDetalle`, `updateaDeposito`, `eliminar`, `setEstado`) **no se ejecutaron** para no alterar datos de otra empresa: el hallazgo ahí es lo que dice el código. Es el mismo gap que ADR-012 detectó en el DataService, del lado del PHP | Barrido de `models/*.php` en `origin/develop` del submódulo: `Articulos`, `Notapedidos`, `Ordeninsumos`, `Pedidoextra`, `Remitos`, `Lovs`, `new/Pedidosmateriales` | en issue #479 — requiere decisión de alcance |
| H-038 | 2026-08-25 | DocTest F3 | traz-tools / submódulos | deuda | 🟡 | **Cuatro submódulos de `develop-v3` están atrás de `develop`, que es lo que corre en el DEMO**: `traz-comp-almacenes`, `traz-comp-bpm`, `traz-comp-pan` y `traz-prod-trazasoft`. El de almacenes está 25 commits atrás.** Entre esos commits está "avances movimientos internos", que es una pantalla del menú (`Movimientos Internos`) que **existe y funciona en el DEMO** pero cuyo controlador **no está** en el árbol que ve `develop-v3`. Relevar contra el árbol local habría dado un catálogo incompleto: este relevamiento se hizo contra `origin/develop` del submódulo | `git log cb63bf1..origin/develop` en el submódulo = 25 commits; `controllers/Movimientointerno.php` solo existe en `origin/develop` | en issue #481 |
| H-039 | 2026-08-25 | DocTest F3 | traz-comp-almacenes / pedidos | bug | 🔴 | **`empr_id` hardcodeado en 1** al guardar un pedido extraordinario: `$peex['empr_id'] = 1; //!HARDCODE`. Todos los pedidos extraordinarios de todas las empresas quedan registrados como de la empresa 1. El resto del controlador usa `empresa()` correctamente | `controllers/Notapedido.php::pedidoExtraordinario()` | en issue #480 |
| H-040 | 2026-08-25 | DocTest F3 | traz-tools / menú | mejora | 🟢 | La opción de menú "Stock Valorizado" tiene un **tabulador dentro del destino**: `linkTo("traz-comp-almacenes/Stockvalorizado	")`. **Verificado: no rompe nada** —el navegador lo normaliza y la pantalla abre—, pero es suciedad en el dato del menú | Menú de Tools en el DEMO, opción Almacenes → Stock Valorizado | abierto |
| H-041 | 2026-08-25 | DocTest F3 | traz-comp-almacenes / entregas | bug | 🟡 | **Que no se entregue más de lo pedido lo protege la pantalla, no el servidor.** La vista oculta el botón de agregar entrega cuando `cant_pedida <= cant_entregada` o `cant_disponible == 0`, pero `insert_entrega_materiales()` guarda el `resto` que venga del formulario sin recalcularlo. Es el mismo patrón que en DNATO (H-017 a H-020): la vista alcanza para el uso normal, falta la defensa del lado del servidor. **El PM confirmó al validar que no se puede entregar de más** (ALM-UC-010), así que la regla existe y no está aplicada donde corresponde | `models/Ordeninsumos.php::insert_entrega_materiales()` vs `views/proceso/tareas/pedido_materiales/view_entrega_pedido_pendiente.php:53` | en issue #483 |
| H-042 | 2026-08-25 | DocTest F3 | traz-comp-almacenes / stock | bug | 🟡 | `Ordeninsumos::actualizar_lote()` descuenta con `SET cantidad = cantidad - X` sin verificar que el lote tenga esa cantidad ni acotar el resultado a cero. Combinado con H-041, una entrega armada a mano deja el lote en negativo. **El PM confirmó que nunca se puede sacar más de lo que hay** (ALM-UC-016), y en movimientos internos esa regla sí está — pero también solo en la pantalla | `models/Ordeninsumos.php::actualizar_lote()`; comparar con la verificación de `views/depositos/MovimientoSalida.php:674` | en issue #483 |
| H-043 | 2026-08-25 | DocTest F3 | traz-comp-almacenes / pedidos | deuda | 🟡 | **Editar un pedido de materiales quedó obsoleto pero el código sigue expuesto.** El PM declaró el 2026-08-25 que hoy un pedido no se puede editar, y efectivamente la pantalla no ofrece el camino; pero `editarPedido()`, `editPedidoV2()`, `editarDetalle()`, `eliminarDetalle()` y `editaDeposito()` siguen siendo métodos públicos alcanzables por URL, y ninguno verifica ni la empresa ni el estado del pedido. `editPedidoV2()` además borra todo el detalle y lo vuelve a insertar | `controllers/Notapedido.php`, caso ALM-UC-008 marcado obsoleto | en issue #484 |
| H-044 | 2026-08-25 | DocTest F3 | traz-comp-almacenes / ajustes | mejora | 🟡 | **Los tipos de ajuste no tienen pantalla de administración.** `Tipoajustes::obtenerAjustes()` los trae de `REST_ALM /stock/ajuste/tipo/list` y no hay ningún ABM en el frontend para crearlos o editarlos: quien necesite un motivo que no está —y en el ajuste el motivo es lo que explica la diferencia— no lo puede agregar solo. **Pesa especialmente en el plan gratuito, donde no hay soporte a quien pedírselo.** Mientras tanto, la ayuda dice que se elija el más parecido y se aclare el resto en la justificación | `models/general/Tipoajustes.php`, `controllers/general/Tipoajuste.php` (solo tiene `obtenerAjuste()`) | abierto |
| H-045 | 2026-08-25 | DocTest F3 | traz-comp-almacenes + core / permisos | mejora | 🟡 | **Tener el rol de almacén no alcanza para ver stock, y nada lo explica en pantalla.** El acceso al menú lo da el rol, pero *qué* stock se ve lo decide `core.encargados_depositos`: cuatro modelos filtran por el usuario encargado (`Articulos`, `Lotes`, `Ordeninsumos`, `Depositos`). Un usuario con el rol pero sin ser encargado de ningún depósito ve el menú completo y **todas las pantallas de stock vacías**, sin ningún mensaje que diga por qué. Los encargados se cargan en el ABM de Establecimientos, dentro de cada depósito. Sería barato mostrar un aviso del tipo "no sos encargado de ningún depósito" en vez de una grilla vacía | `models/Ordeninsumos.php:194`, `models/Lotes.php:333`, `models/Articulos.php:97`, `models/Depositos.php:77`; alta en `core/Establecimiento::guardarDeposito()` | abierto |
| H-046 | 2026-08-25 | DocTest F3 | traz-tools + BD / alta de empresa | bug | 🟡 | **El alta de empresa no crea diez de los parámetros de `core.tablas` que usa Almacenes.** El trigger sí crea `unidades_medida` y `tipo_articulo` —verificado que existen en la empresa de test—, pero no `remito_logo`, `remito_direccion`, `remito_telefono`, `remito_email`, `texto_pie_remito`, `logo_pdf_stock`, `logo_pdf_movimientointerno`, `logo_impresion_recepcion_materiales`, `logo_impresion_entrega_materiales` ni `alm_configs`. **Verificado en vivo**: `getDatosCabeceraRemito` devuelve `{"logo":null,"direccion":null,"telefono":null,"email":null,"texto_pie_remito":null}`. No rompe, pero **toda empresa nueva imprime remitos sin logo ni datos de contacto**, y la valorización del stock queda apagada sin que nadie sepa que existe. Como la lista no existe, tampoco aparece en el ABM de Lista de Valores para completarla | `configuracion_inicial_empresa_trg_func.sql:69-85` vs los `obtenerTablaEmpr_id()` de `Movimientointerno`, `Movimientodeposalida`, `Remito`, `Lote`, `new/Entrega_Material` | en issue #485 |
| H-047 | 2026-08-25 | DocTest F2 | traz-prod-assetplanner | bug | 🟡 | **Quedaron llamadas de depuración activas que escriben HTML en la respuesta.** `dump()` no es un log: imprime un `<pre>` amarillo en la salida (`general_helper.php:19`, con `$echo = TRUE`). Hay **más de 15 llamadas activas en 8 archivos**, entre ellas `Componente::guardar_componente()` —el alta de componentes del piloto de F2— y varias del circuito de OT (`Ordeninsumo`, `Tarea`, `Ordeninsumos`, `Calendarios`). Donde la respuesta se consume por AJAX esperando JSON, ese HTML la corrompe. **No verificado en pantalla**: falta un usuario de AssetPlanner en el DEMO (H-048), así que el hallazgo es lo que dice el código | `Componente.php:216,496`, `Ordeninsumo.php:129-141`, `Plantillainsumo.php:75,90`, `Tarea.php:708`, `Calendarios.php:777`, `Ordeninsumos.php:182,197`, `UnidadMedidas.php:63` | en issue #486 |
| H-048 | 2026-08-25 | DocTest F2 | traz-prod-assetplanner / acceso | deuda | 🟡 | **AssetPlanner tiene su propio padrón de usuarios y no comparte la sesión de Tools.** Verificado en el DEMO: la app muestra su propio formulario de ingreso, y las credenciales de Tools (`jperez@prueba.com`) devuelven *"Error! Revise los datos de acceso ingresados"*. Los usuarios viven en la tabla `sisusers` de asset, no en `seg.users` de Dnato. **Bloquea la verificación en vivo y los tests E2E de MAN** hasta tener un usuario del DEMO. Además, para DocTest implica que MAN necesita su propia fixture de sesión: el `storageState` de Tools no sirve | Login de `demo.cloudtrazalog.com/tools/traz-prod-assetplanner`; `tests/e2e/README.md` del repo de asset documenta `ASSET_USER`/`ASSET_PASS` contra `sisusers` | explicado por H-049 — la causa es el hash, no un dato faltante |
| H-049 | 2026-08-25 | DocTest F2 | traz-tools + AssetPlanner / identidad | bug | 🔴 | **Ningún usuario creado por la registración puede entrar a AssetPlanner: la contraseña se guarda en texto plano y asset la compara contra MD5.** El alta sí llega a `sisusers` —el circuito existe—, pero `setUserAsset` hace `INSERT … values (:pass …)` sin transformar, mientras `Apps::sessionStart_()` busca `usrPassword = md5($pass)`. **El mismo `toolsCOREAPI` lo hace bien en el otro camino** (`POST /usuario/bpm-asset` manda `password_md5`); falta en `POST /usuario`, que es el de la registración. **Verificado contra el DEMO**: cinco combinaciones de la empresa de test —incluido el administrador y los cinco usuarios por defecto con clave `12345`— todas rechazadas. Una empresa que se registra hoy **no puede usar Mantenimiento**, y sus contraseñas quedan en claro | `COREDataService.dbs` query `setUserAsset` (idéntico en develop, develop-v3 y master); `toolsCOREAPI.xml` `POST /usuario` vs `POST /usuario/bpm-asset` — **verificado en las tres ramas y en las dos copias del archivo, incluida la del proyecto Maven que es la que se despliega**; `traz-prod-assetplanner/application/models/Apps.php::sessionStart_()` | **RESUELTO** — el hash esta bien: la base del DEMO muestra `md5('12345')` correcto (2026-09-10). Lo que quedaba sin resolver era otra cosa, ver H-085 |
| H-050 | 2026-08-25 | DocTest F2/F3 | ayudas legacy | deuda | 🟡 | **El manual de ayuda publicado describe dos comportamientos que el sistema no tiene.** `manual_almacen_mantenimiento.html` §02 dice que "al ejecutar una OT, el Mantenedor selecciona los artículos utilizados y las cantidades" y que "el sistema descuenta el stock automáticamente", y que al caer bajo el punto de pedido "el sistema genera una alerta para reponer". En realidad: el informe de servicio **no persiste los materiales consumidos** —muestra lo pedido rotulado "Insumos Usados", con un TODO en el código que lo admite— y el descuento de stock ocurre en la **entrega de almacén**, no al ejecutar la OT; y **no hay alerta**: hay una pantalla de Punto de Pedido que alguien tiene que ir a mirar (verificado en el DEMO). Es exactamente lo que DocTest previene: la ayuda nueva se deriva de casos validados. **El legacy no se corrige** —se conserva tal cual, es una URL viva— pero conviene saber que dice eso mientras conviva con la ayuda nueva | `ayudas/legacy/manual_almacen_mantenimiento.html` §02 vs `circuitos-man-alm-pan.md` §Circuito 4 y el relevamiento de ALM-UC-015 | abierto |
| H-051 | 2026-08-25 | DocTest F2 | traz-tools / _backend | deuda | 🟡 | **Los artefactos del backend están duplicados en varias rutas y las copias no coinciden.** El `COREDataService.dbs` está en **cuatro**: `ToolsAPIProject` (92 queries, tocado el 2026-08-25 — **es el vivo**), `_backend/api/dataservice/` (90 queries, 2026-08-19 — dos queries atrás) y dos dentro de `ToolsCoreAPIProject` (**6 queries**, sin tocar desde marzo y sin `setUserAsset`: desplegar desde ahí destruiría el servicio). Y `toolsCOREAPI.xml` está dos veces y las copias no coinciden.** Existe `_backend/api/toolsCOREAPI.xml` (suelto) y `_backend/api/ToolsAPIProject/.../artifacts/apis/toolsCOREAPI.xml` (el del proyecto Maven, **que es el que se compila y despliega**). Difieren en 28 líneas: al suelto **le falta el paso 3.c** que vincula `empr_id_mysql` en `core.empresas`. El suelto es el que uno abre primero por su ruta, así que se lee la versión vieja creyendo que es la que corre. **Verificado además que el archivo es idéntico entre `develop`, `develop-v3` y `master`** — no hay una rama "buena" y otra "pisada" | `diff` entre las dos copias; `git diff origin/develop origin/develop-v3 -- _backend/api/toolsCOREAPI.xml` sin salida | en issue #490 |
| H-052 | 2026-08-26 | DocTest F2 | traz-tools / _backend | bug | 🔴 | **El alta de empresa quedó rota tras actualizar el `toolsCOREAPI`: el API nuevo llama a un recurso que el DataService desplegado no tiene.** La versión del proyecto Maven agrega el paso 3.c, que hace `PUT /COREDataService/empresa/asset-id` para vincular `empr_id_mysql`. Ese recurso **existe solo en el `COREDataService.dbs` del mismo proyecto Maven**, no en `_backend/api/dataservice/COREDataService.dbs`. Si el servidor tiene desplegado el DataService viejo, ese PUT no encuentra a quién llamar. **Verificado en vivo dos veces**: el alta falla en el último paso con *"No hubo conexión con el servidor de aplicaciones"*, que en el PHP corresponde a `code === 0` — o sea **ninguna respuesta**, ni siquiera un error. Y **WSO2 está vivo**: el combo de empresas del login se llena, `core/Valor/getValor` y los artículos responden 200. Es específico de `POST /empresa`. Es la consecuencia directa de H-051: dos rutas para el mismo artefacto, y se desplegó una sin la otra | `toolsCOREAPI.xml` del proyecto Maven, paso 3.c (`PUT /empresa/asset-id`) vs `grep 'path="/empresa/asset-id"'` en las dos copias del `.dbs`; `Register.php:714` | en issue #491 — bloquea el alta de empresas |
| H-053 | 2026-08-26 | DocTest F2 | ayudas legacy / MAN | deuda | 🟢 | **El manual de Alta de Equipos publicado quedó desactualizado en tres puntos**, verificados contra el DEMO: dice que las listas desplegables *"deben cargarse previamente desde el ABM correspondiente"* cuando **cada combo tiene su botón de agregar** (`addarea`, `addproceso`, `addcriti`, `addetapa`, `addgrupo`, `addcliente`, todos visibles); marca como obligatorios Código, Marca, Descripción y N° de Serie cuando **la pantalla pone el asterisco en Área, Proceso, Criticidad y Sector/Etapa**; y habla de "Garantía (meses)" cuando el campo es una **fecha**. Además no menciona la *Fecha de Lectura Inicial*. Mismo caso que H-050 en ALM: el legacy no se corrige —es URL viva— pero la ayuda nueva sí lo dice bien | `ayudas/legacy/manual_alta_equipos_componentes.html` vs el formulario de `Equipo` en el DEMO, verificado el 2026-08-26 | abierto |
| H-054 | 2026-08-26 | DocTest F2 | traz-prod-assetplanner / equipos | mejora | 🟢 | **Ningún campo del alta de equipos declara `required` en el HTML**, aunque la pantalla marca cuatro con asterisco. Si hay validación, está en el JavaScript: es el mismo patrón de DNATO (#456, #459-461) y ALM (#483) — la vista alcanza para el uso normal y el servidor no revalida. Queda por confirmar además **cuáles son los obligatorios de verdad**, porque la pantalla y el manual de ayuda no coinciden (H-053) | Formulario de `Equipo` en el DEMO: los `<select>` de área, proceso, criticidad y etapa llevan asterisco en la etiqueta, ninguno lleva el atributo | abierto |
| H-055 | 2026-08-26 | DocTest F2 | traz-prod-assetplanner / equipos | mejora | 🟡 | **El código del equipo se puede cambiar después de creado, y no debería.** Decisión del PM (2026-08-26): el código no tendría que ser editable una vez que el equipo tiene historial —órdenes, lecturas, informes lo referencian—; a lo sumo debería poder cambiarlo un Administrador para corregir una carga mal hecha. Hoy la pantalla de edición lo deja modificar a cualquiera. **Mejora a analizar**, no bug | `Equipo::editar_equipo()`, caso MAN-UC-003 | abierto |
| H-056 | 2026-08-26 | DocTest F2 | traz-prod-assetplanner / equipos | bug | 🟡 | **La unicidad del código de equipo no parece estar garantizada del lado del servidor.** El PM confirmó que el código debe ser único dentro de la empresa y que repetirlo tiene que dar error; pero `Equipos::getcodigo()` trae **todos** los equipos sin filtrar, lo que sugiere que la verificación de duplicados la hace el JavaScript de la pantalla. Es el mismo patrón que en DNATO (#456, #459-461) y ALM (#483). **Falta confirmar dónde se guarda el equipo** para ver si valida: no se ubicó el método de guardado en el controlador | `Equipos::getcodigo()` sin `where` de empresa; caso MAN-UC-002 | abierto — falta confirmar |
| H-057 | 2026-08-26 | DocTest | ayudas / theme | bug | 🟢 | **Los encabezados de tabla salían blancos sobre blanco.** `.compare-table th` define `color: white` **sin fondo**: en los manuales publicados cada `<th>` lo resolvía con un `style="background:var(--red)"` inline, así que cualquier tabla que se olvidara quedaba con el texto invisible. Pasó en los tres manuales nuevos —Mantenimiento, Almacenes y Registración—, y se detectó al revisar el sitio completo renderizado. Corregido en el theme con un fondo por defecto para `thead th`; los inline del legacy siguen ganando, así que sus tablas de colores no cambian | `ayudas/plantilla/theme.css:450` vs `ayudas/legacy/manual_mantenimiento_correrctivo.html` | corregido |
| H-058 | 2026-08-26 | DocTest F2 | ayudas | bug | 🟡 | **El sitio de ayudas ofrece dos manuales para cada tema, y se contradicen entre sí.** El índice lista 8 manuales: los 3 nuevos derivados del catálogo y los 5 publicados antes — y **los 5 viejos están cubiertos por los 3 nuevos**: *Alta de Equipos*, *Correctivo* y *Preventivo* por el de Mantenimiento; *Almacén de Mantenimiento* por el de Almacenes; *Configuración Inicial* por el de Registración. Un usuario que entra no sabe cuál leer, y las versiones no dicen lo mismo (H-050, H-053). Hay que dejar un solo manual por tema en el índice | `ayuda/index.html`: 8 tarjetas para 3 temas | corregido — el índice deja un solo manual por tema y los legacy avisan cuál los reemplaza (commit `07de68a6`) |
| H-059 | 2026-08-26 | DocTest F2 | traz-prod-assetplanner / preventivo | mejora | 🟢 | **Las horas hombre del preventivo se calculan solas y el manual nuevo decía que se cargaban.** El campo `hshombre` está `disabled` y `calcularHsHombre()` lo computa desde duración × unidad × cantidad de operarios — ocho horas con dos operarios son 16. El manual de ayuda anterior lo explicaba bien y el nuevo lo había perdido: corregido. **Es un buen recordatorio de que el legacy no siempre se equivoca**, y conviene leerlo antes de reescribir un tema | `views/preventivo/list.php:594,992`; `manual_mantenimeinto_preventivo.html` §04 | corregido |
| H-060 | 2026-08-26 | DocTest F2 | traz-prod-assetplanner / plan | mejora | 🟡 | **La programación en serie de preventivos existe en el código, pero al validar el caso se indicó que las órdenes se generan "de a una".** `Calendario::setOTenSerie()` con `cant_meses` está activo y el manual de ayuda anterior lo documenta: *"Mensual + Cantidad de Meses genera automáticamente OTs a futuro para todos los meses del período"*. El manual nuevo pasa a describir las dos formas —una por vez como lo habitual, y la serie como opción— pero **conviene confirmar si la serie se sigue usando o quedó en desuso**: si es lo segundo, corresponde sacarla de la pantalla | `Calendario.php:165,370,394`; validación de MAN-UC-009 (2026-08-26) | abierto — requiere confirmación |
| H-061 | 2026-09-01 | DocTest | traz-comp-dnato / carga masiva | deuda | 🟡 | **La carga masiva de Mantenimiento depende de un vinculo que el alta de empresa puede no haber escrito.** Las entidades de Mantenimiento se cargan contra la base de AssetPlanner, y para eso `resolverEmprIdAssetPlanner()` traduce la empresa de la sesion usando `core.empresas.empr_id_mysql`. Ese campo lo escribe el alta de empresa por el recurso `PUT /empresa/asset-id`, que es lo que el issue #491 tiene roto: **toda empresa creada mientras #491 estuvo caido no tiene el vinculo, y su carga masiva de equipos va a fallar** con "la empresa no tiene definido su vinculo". No es un defecto del despacho —avisa correctamente— sino datos incompletos que hay que completar empresa por empresa | `Bulkloads.php:1020+ resolverEmprIdAssetPlanner()`, `core.empresas.empr_id_mysql`, issue #491 | abierto — supeditado a #491 |
| H-074 | 2026-09-09 | DocTest | traz-tools / dataservices | deuda | 🟡 | **Ocho ids duplicados dentro de los propios DataServices, preexistentes.** En un `.dbs`, dos queries con el mismo `id` o dos resources con el mismo metodo y path hacen que uno tape al otro sin aviso: **cual gana depende del orden en el archivo**, y nadie lo esta decidiendo a proposito. Los ocho: `extraerCantidadLote`, `GET /establecimientos/empresa/{empr_id}` y `POST /articulo` en ALMDataService; `POST /entradas` en LOGDataService; `getRecursos`, `setEntrada`, `POST /entradas` y `POST /lotes/movimiento_stock` en PRDDataService. **No los introdujo la unificacion** —verificado comparando contra el estado anterior en git—, ya estaban. Conviene revisarlos uno por uno: en cada caso hay una version que sobra o dos que deberian tener nombres distintos | verificado el 2026-09-09 sobre `_backend/api/dataservice/*.dbs` | abierto |
| H-075 | 2026-09-10 | DocTest | traz-tools / artefactos WSO2 | bug | 🔴 | **`toolsBPMAPI.xml` no se desplegaba: el archivo y el `name` que declara no coincidian.** Synapse identifica el artefacto por su atributo `name`, no por el nombre del archivo. El archivo se llamaba `toolsBPMAPI.xml` y declaraba `name="toolsbpmAPI"`, con la b minuscula. En el servidor ya habia una API registrada con ese nombre desde otro archivo, asi que el nuevo quedaba huerfano y fallaba en cada barrido del deployer con `Duplicate resource definition by the name: toolsbpmAPI` — verificado en `wso2carbon.log` del DEMO. **La API de BPM seguia corriendo la version vieja.** Corregido renombrando el archivo y no el `name`: cambiar el `name` habria dejado dos APIs sobre el mismo `context` `/tools/bpm`. Nadie referencia la API por nombre; el PHP la llama por context (`REST_BPM`) | `wso2carbon.log` 2026-09-09 21:10:36; `artifacts/apis/toolsBPMAPI.xml` | corregido — falta borrar el huerfano del servidor |
| H-076 | 2026-09-10 | DocTest | traz-tools / artefactos WSO2 | deuda | 🟡 | **Cuatro sequences mas tienen el mismo desajuste entre archivo y `name`, latente.** `EmprIdFromHeader.xml` declara `emprIdFromHeader`, `EmprIdInjector.xml` declara `emprIdInjector`, `JwtValidator.xml` declara `jwtValidator` y `toolsFaultSequence.xml` declara `toolsFault`. Hoy no rompen porque hay **un** archivo por nombre, pero es la misma trampa que dejo la API de BPM sin desplegar: alcanza con que aparezca un segundo archivo declarando ese nombre. Arreglarlo NO es solo renombrar en el repo — hay que borrar el archivo viejo del servidor en la misma tanda, o quedan los dos y se produce el duplicado que se queria evitar. El script de despliegue ahora lo avisa en pantalla | verificado el 2026-09-10 sobre `artifacts/sequences/` | abierto — requiere paso manual coordinado |
| H-077 | 2026-09-10 | DocTest F2 | traz-comp-dnato + traz-tools / alta de empresa | bug | 🔴 | **El alta de empresa se cuelga 30 s y no da error.** Medido: `register/guardarEmpresa` vuelve en **30,2 s** —el `CURLOPT_TIMEOUT` de `REST.php:66`— con `0 out of -1 bytes received`, o sea que WSO2 nunca empezo a responder. **CORREGIDO EL DIAGNOSTICO ORIGINAL**: se penso que la causa era que el `POST /empresa` encadena ~41 llamadas y no llegaba a tiempo. **Es falso**: el `wso2carbon.log` muestra que la cadena hace todo en **1,5 segundos** —crea grupo, rol y actorMember sin despeinarse—. Las causas reales son otras dos, y estan separadas en H-079 (el `toolsFault` roto, que convierte cualquier error en un cuelgue) y H-080 (el alta exigia seis procesos de Bonita publicados). Se deja la ficha porque el sintoma medido —30,2 s— es el que se ve desde afuera y es por donde se entra a diagnosticar | `wso2carbon.log` del DEMO 2026-09-10 01:13:12; `REST.php:62,66`; seed con `DOCTEST_SEED_TRACE=1` | explicado por H-079 y H-080 |
| H-077 | 2026-09-10 | DocTest F2 | traz-comp-dnato + traz-tools / alta de empresa | bug | 🔴 | **El alta de empresa no falla: tarda mas de 30 s y PHP le corta la llamada.** El log del DEMO del 2026-09-09 lo dice sin ambiguedad: `Operation timed out after 30001 milliseconds with 0 out of -1 bytes received`, y acto seguido `#TRAZA\|REGISTER\|guardarEmpresa() >> Fallo HTTP/transporte \| body_snip=` **vacio**. Los 30001 ms son exactamente el `CURLOPT_TIMEOUT = 30` de `REST.php:66`; el `0 out of -1` significa que **WSO2 no llego a empezar a responder** — no es un corte de red ni un error del API, es que no termina a tiempo. La causa esta a la vista en el artefacto: el recurso `POST /empresa` de `toolsCOREAPI` es **una sola cadena sincronica de 41 llamadas HTTP salientes** — 10 `<call>` directos (alta en core, alta en asset, vinculo `asset-id`, alta en BPM, perfil, y los `DELETE` de rollback), mas **16 invocaciones a `toolsCreateRole`** y **15 a `toolsBpmActorMembership`/`toolsBpmActorGrupo`**. Y cada una de esas 31 no va a Bonita directo: va a `{api_url}/bpm/role` — o sea **WSO2 llamando a otra API de WSO2** — que recien ahi llega a Bonita por `bpmAPICallTemplate`, el cual ademas **rehace el `loginservice` cada vez que le vuelve un 401**. Eso explica las dos lineas que aparecen pegadas al timeout en el log (`/bonita/loginservice` y `/bonita/API/identity/user?p=0&c=50000`). **El alta probablemente si se completa del lado del servidor**, minutos despues, pero el usuario ya vio el cartel de error y la empresa queda a medio hacer sin que nadie lo sepa. **Subir el timeout de cURL tapa el sintoma**: lo que hay que discutir es partir el recurso —crear la empresa y devolver, y hacer roles y membresias despues— porque son 41 llamadas encadenadas que crecen con cada rol nuevo. **CONFIRMADO por medicion el 2026-09-10** sobre v2.5.1.2, cronometrando el POST desde el navegador: `register/guardarEmpresa` vuelve en **30,2 s**, mientras que `register/guardarFormularioRegistro` —que en la misma corrida contesta bien— vuelve en **0,2 s**. O sea: lo que falla rapido falla rapido; este pega exactamente contra la pared de los 30 s. No hace falta SSH para verlo, lo mide el propio seed con `DOCTEST_SEED_TRACE=1` | `Register.php:568` -> `Empresas::agregarEmpresa()` -> `POST API_CORE/empresa`; `REST.php:62,66`; `apis/toolsCOREAPI.xml` lineas 48-457; `sequences/toolsCreateRole.xml`, `sequences/toolsBpmActorMembership.xml`; `templates/bpmAPICallTemplate.xml:123` | en issue #516 — bloquea el alta de empresas |
| H-078 | 2026-09-10 | DocTest F2 | traz-comp-formularios / instancias | bug | 🟡 | **Los campos de seleccion multiple de un formulario dinamico no se guardan al actualizar, y el sistema informa exito igual.** `Forms::actualizar()` pasa el valor tal cual al query builder; cuando el campo es un array —el input se llama `campo[]`— PHP lo convierte a la cadena `Array` y PostgreSQL rechaza la sentencia completa: `Invalid query: UPDATE "frm"."instancias_formularios" SET "valor" = Array WHERE "info_id" = 750 AND "name" = 'actividad_empresa'`. **Y nadie se entera**: `actualizar()` no miraba el resultado de `update()` y devolvia el `info_id` siempre, asi que dos lineas mas abajo el log dice *"guardarFormularioRegistro: Formulario guardado exitosamente"*. En la registracion el campo afectado es **Actividad de la empresa**, o sea el dato que define el rubro del cliente nuevo. El formato correcto no es una fila por opcion: en este camino `generarInstancia()` crea **una sola fila vacia por campo** y el re-dibujado marca cada opcion buscandola como substring de `valor` (`form_helper.php:133`), asi que el conjunto va concatenado — es lo que ya hace `Form::guardarJson()`. **El submodulo esta apuntado al mismo commit desde `traz-tools` y desde `traz-comp-dnato`**, asi que afecta a los dos y las dos necesitan el bump del puntero | `models/Forms.php::actualizar()`; `Register.php:221-237`; log del DEMO 2026-09-09, info_id 750 | en issue #517 — **corregido en el PR #29 de traz-comp-formularios** |
| H-079 | 2026-09-10 | DocTest F2 | traz-tools / artefactos WSO2 | bug | 🔴 | **`toolsFault` arma JSON invalido y por eso las APIs se cuelgan en vez de dar error.** La plantilla mete el cuerpo entrante DENTRO de comillas —`"payload":"$3"` con `literal="false"`— asi que cuando el cuerpo es un objeto JSON, que es el caso normal cuando hay un error, queda `"payload":"{"respuesta":...}"`. Verificado armando la cadena: revienta en el caracter 70. Entonces `PayloadFactoryMediator` lanza `Existing json payload is malformed`, el `faultSequence` del recurso vuelve a llamar a `toolsFault` y lanza otra vez, y la excepcion termina como `Uncaught exception` en el worker pool **sin que nadie mande respuesta**. El cliente queda colgado hasta su propio timeout: PHP corta a los 30 s (`CURLOPT_TIMEOUT`), WSO2 recien suelta a los 120 (`GLOBAL_TIMEOUT`) y cierra el socket a los 180. **`toolsFault` lo usan TODAS las APIs**, asi que cualquier error en cualquier flujo se ve igual: 30 segundos de silencio y "No hubo conexion con el servidor de aplicaciones". Eso es lo que hizo que #491 y H-077 parecieran el mismo problema durante dias. Corregido: `"payload":$3` con `literal="true"` | `sequences/toolsFaultSequence.xml`; `wso2carbon.log` del DEMO 2026-09-10 01:13:12-01:16:12 | **corregido en el PR #521** — requiere despliegue manual del artefacto |
| H-080 | 2026-09-10 | DocTest F2 | traz-tools / alta de empresa | bug | 🟡 | **El alta de empresa exigia que estuvieran publicados SEIS procesos de Bonita, incluidos tres de un modulo que esas empresas nunca usan.** El mapeo de actores recorria `Pedido de Recursos Materiales` (Almacenes), `proceso de Mantenimiento AssetPlanner`, `TST001 - Tarea Planificada` y los tres `TERSU-BPM01/02/03` (residuos). **Si faltaba uno solo, el alta moria** — y por H-079 moria colgada 30 s sin decir cual. Verificado en vivo el 2026-09-10: `TST001` no estaba publicado en el DEMO y el alta fallaba; al publicarlo, paso a fallar en el siguiente. **Decision del PM**: obligatorios son solo Mantenimiento, Almacenes y TST001; TERSU y Yudi se dan de alta a mano y esas empresas jamas se registran solas. Se sacaron los 8 mapeos de TERSU. **Decision ampliada del PM el mismo dia**: los roles y mapeos NO obligatorios no se borran, se vuelven **configurables por instancia** en el `constants.php` de dnato (`ALTA_EMPRESA_ROLES_EXTRA` / `ALTA_EMPRESA_ACTORES_EXTRA`), porque residuos corre **on-premise en el cliente**, donde esas empresas SI se crean y SI los necesitan. El API queda con 12 roles y 3 procesos; los 4 roles `SMA - *` y los 8 mapeos de TERSU pasan a configuracion. Beneficio de fondo: cambiar esa lista deja de requerir un despliegue manual de artefacto WSO2. Y **si un extra declarado falla, el alta se revierte completa** —criterio del PM: *"opcional" es por instancia, no por empresa; una empresa de residuos sin roles ni actores no sirve para nada, asi que crearla igual seria peor que no crearla*. Se recorre la lista entera antes de cortar para que el log liste TODOS los que fallaron. **Corolario a tener presente**: un proceso declarado que no este publicado y habilitado en Bonita impide registrar cualquier empresa nueva en esa instancia | `apis/toolsCOREAPI.xml` `POST /empresa`; `constants.php` de dnato; corrida del 2026-09-10 02:06 | **PR #521 (tools) + PR #40 (dnato)** — el #521 requiere despliegue manual de dos artefactos |
| H-081 | 2026-09-10 | DocTest F2 | traz-prod-assetplanner + traz-tools / baja de empresa | bug | 🔴 | **La baja de empresa es logica en Tools y FISICA en AssetPlanner: son dos criterios distintos para la misma operacion.** Regla de negocio del PM (2026-09-10): *una baja puede ser por suspension de pago, y si el cliente vuelve a pagar no se puede haber perdido el historial de procesos; por eso no se da de baja real*. Tools la cumple —`deleteEmpresa` hace `UPDATE core.empresas SET eliminado = TRUE`—, pero **AssetPlanner borra de verdad**: `deleteEmpresaAsset` es `DELETE FROM empresas WHERE id_empresa = ...` sobre MariaDB, y el propio AssetPlanner tiene otros dos caminos fisicos, `Empresas::eliminacion()` —alcanzable desde `Empresa.php:64`— y el `case 'Del'` de `guardar()`. Una empresa suspendida perderia **equipos, ordenes de trabajo, informes y lecturas** del lado de mantenimiento, que es justo el historial que la regla protege, y volveria irreversible una baja que el negocio define como reversible | query `deleteEmpresa` vs `deleteEmpresaAsset` en `COREDataService.dbs:320,392`; `traz-prod-assetplanner/application/models/Empresas.php:112,124` y `controllers/Empresa.php:64` | abierto |
| H-082 | 2026-09-10 | DocTest F2 | traz-tools / alta de empresa | deuda | 🟢 | **Revertir un alta fallida deja el grupo y los roles creados en Bonita.** El `POST /empresa` crea el grupo `<empr_id>-<empresa>` y hasta 16 roles antes de que `postProcesarEmpresa()` pueda fallar; el rollback marca la empresa como eliminada pero no toca Bonita. **NO corresponde borrarlos**: por la regla de que la baja es siempre logica (H-081), lo que sobrevive en Bonita es lo correcto. Se anota solo como acumulacion: las altas que nunca llegaron a ser operativas dejan artefactos que nadie va a usar, y los nombres llevan el `empr_id`, que no se reusa. Es cosmetico y no urge; si algun dia molesta, lo que corresponde es una limpieza off-line de empresas que quedaron `eliminado=TRUE` sin ninguna actividad, no un DELETE en el camino del alta | `apis/toolsCOREAPI.xml` `POST /empresa`; `Register.php:598-612` | abierto — no accionable por ahora, a proposito |
| H-085 | 2026-09-10 | DocTest F2 | traz-tools + AssetPlanner / identidad | bug | 🔴 | **Ningun usuario creado por Tools puede entrar a AssetPlanner: falta la fila que lo vincula con su empresa.** El login —`traz-prod-assetplanner/models/Users.php:236`— no valida solo usuario y clave: hace `JOIN usuarioasempresa` y exige `tipo = 1`. Y **nada en Tools escribe esa tabla**: las unicas cuatro queries del proyecto contra la base de asset son `validaBPMCaseYEmprAsset`, `setUserAsset`, `setEmpresaAsset` y `deleteEmpresaAsset`; cero menciones a `usuarioasempresa` en los 21 `.dbs`, las sequences, las apis y **todo el historial de git de los tres repos**. Lo unico que la escribe es el ABM interno de AssetPlanner (`Users.php:190`). **Esto es lo que estuvo tapado detras de #489 durante semanas**: se creia que era el hash de la contrasena, pero la base muestra `827ccb0eea8a706c4c34a16891f84e7b`, que es exactamente `md5('12345')` — ese bug ya estaba arreglado. Afecta por igual a la registracion y al ABM. **Corregido**: el vinculo pasa a ser parte de `POST /rol/asignar`, con el mapeo rol -> grupo en la sequence `toolsAssetUserEmpresa`, asi los tres caminos que asignan roles quedan cubiertos sin tocar pantallas | `Users::sessionStart_()`; conteo de queries sobre `AssetPlannerDataSource`; captura de `sisusers` del DEMO 2026-09-10 | **corregido en el PR #525 (tools) + #41 (dnato)** |
| H-086 | 2026-09-10 | DocTest F2 | traz-prod-assetplanner / login | mejora | 🟡 | **El login de AssetPlanner no distingue “clave incorrecta” de “usuario sin empresa”.** `Users::sessionStart_()` arma una sola consulta con las tres condiciones —`usrNick`, `usrPassword` y el `JOIN usuarioasempresa` con `tipo = 1`— y si vuelve vacia responde lo mismo en todos los casos. Por eso H-085 se confundio con el bug del hash (#489) durante semanas: el sintoma era identico y no habia forma de distinguirlos desde afuera. Separar la verificacion de credenciales de la de pertenencia costaria poco y ahorraria mucho diagnostico. **Ojo con el reverso**: el mensaje AL USUARIO debe seguir siendo generico, para no revelar si un correo existe; lo que hace falta es que el **log** lo diga | `traz-prod-assetplanner/application/models/Users.php:236-261` | abierto |
| H-087 | 2026-09-10 | DocTest F2 | traz-tools / infraestructura | deuda | 🟡 | **El trigger y el procedure que crean los grupos de cada empresa vivian solo en la base.** `trg_empresas_after_insert` llama a `sp_create_empresa_groups_and_actions`, que crea los cinco grupos (`Admin`, `Supervisor de Taller`, `Solicitante`, `Mantenedor`, `Almacen`) con sus permisos de menu. `toolsCOREAPI.xml:120-127` ya los referenciaba en un comentario apuntando a `development/sql/asset-empresa-trigger.sql`, **y ese archivo no existia**. Si se recreaba el ambiente se perdian, y el alta seguiria creando empresas sin grupos —usuarios que entran y no ven ningun menu— sin que nada lo avisara. **Versionado** con el volcado del DEMO. Queda anotado que los `menuAccId` son ids de `sismenuactions` de esa base y **no son portables** a otra instalacion | `development/sql/asset-empresa-trigger.sql` (nuevo); comentario de `toolsCOREAPI.xml` | **corregido en el PR #525** |

---

## Origen de los hallazgos

| Origen | Qué es |
|---|---|
| `DocTest F1` | Relevamiento del catálogo funcional de DNATO (issue #438) |
| `DocTest F2..F5` | Fases siguientes de DocTest |
| `<tarea>` | Cualquier otra tarea del `STATE.md` que haya encontrado el hallazgo |
