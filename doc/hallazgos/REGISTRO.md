# Registro de hallazgos

## Objetivo

Es el lugar único donde se anotan los **hallazgos** del proyecto: bugs, deudas y mejoras que aparecen mientras se hace otra cosa (un relevamiento, una migración, un despliegue) y que **no se corrigen en esa misma tarea**. Está escrito para el PM, que tría y prioriza, y para Claude Code, que lo alimenta. **No** es un backlog de features (eso son los issues del Project) ni un registro de decisiones (eso es `doc/v3/STATE.md`).

Sin este archivo, un hallazgo vive en la descripción de un PR que se mergea y se pierde de vista. Acá queda con su evidencia y su estado.

---

## Cómo se usa

**Al encontrar algo:**

1. Agregar una fila a la tabla con el próximo `H-NNN`. Nunca se reutiliza un número.
2. Escribir la evidencia concreta: archivo y línea, o la consulta/verificación que lo demuestra. Un hallazgo sin evidencia no entra.
3. **Si es un bug** (el sistema hace algo distinto de lo que dice hacer): además se abre un issue de GitHub con label `hallazgo` + `bug`, y se pone su número en la columna Issue.
4. **Si es una mejora o una deuda**: queda solo acá hasta que el PM decida. Si decide hacerlo, ahí se abre el issue.

**Regla:** el hallazgo se documenta, **no se corrige en la tarea que lo encontró** — salvo que el PM lo pida explícitamente. Corregir de paso mezcla el diff y rompe la revisión.

**Estados:** `abierto` · `en issue #N` · `corregido en #N` · `descartado (motivo)` · `es correcto` (se verificó y funciona como se espera).

**Severidad:** 🔴 datos incorrectos, fuga entre empresas o pérdida de información · 🟡 afecta al usuario pero tiene rodeo · 🟢 cosmético o interno.

> **Lección de método (2026-08-25).** Dos hallazgos de esta tanda —H-017 y H-019— nacieron de leer el controlador sin mirar la vista, y los dos estaban mal: la pantalla ya protegía lo que el controlador parecía dejar abierto. **Un hallazgo de interfaz no se reporta sin verificarlo contra la pantalla real**, y si ya se reportó, se corrige el issue en vez de dejarlo. Lo que sí queda en esos casos es la defensa en profundidad que falta del lado del servidor, que es un hallazgo distinto y de severidad menor.

---

## Hallazgos

| ID | Fecha | Origen | Repo / Módulo | Tipo | Sev | Resumen | Evidencia | Estado |
|---|---|---|---|---|---|---|---|---|
| H-001 | 2026-08-19 | DocTest F1 | traz-comp-dnato / registración | bug | 🔴 | Los cinco usuarios iniciales de cada empresa nacen con la contraseña `12345`, y la pantalla de bienvenida la muestra en pantalla. Nada obliga a cambiarla | `constants.php:230`, `Register::registro_completo()` | en issue #454 |
| H-002 | 2026-08-19 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🔴 | "Eliminar usuario" hace **borrado físico**: `DELETE` sobre `seg.users_business` y después sobre `seg.users`. Se esperaba baja lógica. Además no da de baja al usuario en Bonita ni en Asset, así que queda huérfano ahí. **Decisión del PM (2026-08-24): nunca debe existir un borrado definitivo, porque rompe la trazabilidad de lo que ese usuario hizo. La baja tiene que ser lógica** | `User_model.php:770-795`, `Main.php:693-741` | en issue #451 |
| H-003 | 2026-08-19 | DocTest F1 | traz-comp-dnato + BD | deuda | 🟡 | El superusuario está definido **en dos lugares distintos y desacoplados**: `TOOLS_ADMIN_USER` en `constants.php` (controla el menú de Gestión de Empresas) y `core.tablas.coresuper_admin` (lo usa el trigger de alta de empresa, con `jperez@prueba.com` como default hardcodeado). Si no coinciden, el sistema tiene dos "superusuarios" con poderes distintos. **El trigger además agrega ese usuario a TODA empresa nueva con rol Administrador** — verificado en vivo el 2026-08-24: aparece en la lista de usuarios de la empresa de test recién creada. **El PM lo acepta por ahora**; queda como posible mejora | `constants.php:105`, `configuracion_inicial_empresa_trg_func.sql:15-25,55-60` | abierto — aceptado por ahora |
| H-004 | 2026-08-19 | DocTest F1 | traz-comp-dnato / login | mejora | 🟡 | El combo de empresas del login lista **todas** las empresas del sistema a cualquiera que abra la pantalla, sin sesión | `Main.php:1362-1400` | abierto — lo resuelve el cambio de login previsto en v3 |
| H-005 | 2026-08-19 | DocTest F1 | traz-comp-dnato / login | mejora | 🟢 | Los mensajes de login y recuperación distinguen entre "no existe el correo", "no pertenece a la empresa" y "cuenta no aprobada": permiten averiguar desde afuera si un correo está registrado | `Main.php:1412`, `Main.php:1505-1512` | abierto |
| H-006 | 2026-08-19 | DocTest F1 | traz-comp-dnato | mejora | 🟢 | Código muerto: `Main::associaterol()` carga una vista `membership` que no existe en el repo; `changelevel.php`, `changeleveluser_old.php`, `changeuser_old`, `banuser_old` y `Bulkload copy.php` son versiones anteriores sin enlace en el menú. **El PM declaró obsoleta la pantalla vieja de habilitar/inhabilitar (2026-08-24)** | `Main.php:1012-1030`, `application/views/` | abierto |
| H-007 | 2026-08-19 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🟡 | Editar un usuario **obliga a reescribir su contraseña**: `edituser()` marca contraseña y confirmación como `required`, igual que el alta. Cambiar solo el apellido fuerza una contraseña nueva. **Subsumido por H-035 (2026-08-25): la pantalla de edición ni siquiera existe**, así que estas reglas nunca llegan a mostrarse | `Main.php:742-771` | en issue #457, supeditado a #468 |
| H-008 | 2026-08-19 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🔴 | `edituser()` y `deleteuser()` **no verifican** que el usuario objetivo pertenezca a una empresa del administrador conectado: con el id en la URL, un administrador puede editar o borrar usuarios de otra empresa | `Main.php:693-771` | en issue #453 |
| H-009 | 2026-08-19 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🟢 | El mensaje de error de habilitar/inhabilitar dice "Error al borrar usuario" — texto copiado de otra pantalla | `Main.php:500-554` | abierto |
| H-010 | 2026-08-19 | DocTest F1 | traz-comp-dnato / cuenta | bug | 🟡 | Cambiar la contraseña propia no pide la contraseña actual y, a diferencia de la activación, **no se propaga** a Bonita ni a Asset: lo que sí falta es pedir la contraseña actual antes de cambiarla. **Acotado por el PM (2026-08-24): que no se propague a Bonita ni a Asset NO es un problema — la contraseña la administra Dnato y nadie más**; queda solo la mejora de seguridad | `Main.php:555-610` | abierto — mejora de seguridad |
| H-011 | 2026-08-19 | DocTest F1 | traz-comp-dnato / tokens | bug | 🟢 | El vencimiento de los enlaces de activación y recuperación compara la **fecha** de creación con la de hoy (`createdTS != todayTS`): valen hasta la medianoche, y si la columna guardara hora ningún enlace validaría nunca | `User_model.php:111-135` | abierto |
| H-012 | 2026-08-24 | DocTest F1 | traz-tools + BD / alta de empresa | bug | 🔴 | **Un rol se queda sin menú por una diferencia de nombre**: el alta crea el rol `Responsable de Procesos <empresa>` (en `toolsCOREAPI`) pero el trigger le asigna el menú a `Responsable Procesos <empresa>` (sin "de"). Los dos nombres nunca coinciden, así que ese rol no ve el módulo Procesos | `toolsCOREAPI.xml` (roles del POST /empresa) vs `configuracion_inicial_empresa_trg_func.sql:33-44` | en issue #452 |
| H-013 | 2026-08-24 | DocTest F1 | traz-tools + BD / alta de empresa | bug | 🟡 | El alta de empresa crea **16 roles** pero el trigger asigna menúes a **8**. Quedan sin ningún menú los cuatro de Mantenimiento (Supervisor, Planificador, Solicitante, Mantenedor) y los cuatro de SMA/Residuos (Transportista, Generador, Operario Descarga, Operador de Bascula). Para Mantenimiento puede ser correcto (vive en Asset Planner, aplicación aparte), pero **SMA es un módulo de Tools**. **Acotado por el PM (2026-08-24): que Mantenimiento no reciba menú es correcto —todavía vive en Asset Planner y no está migrado—, así que el hallazgo queda reducido a los cuatro roles de SMA/Residuos** | `toolsCOREAPI.xml` vs `configuracion_inicial_empresa_trg_func.sql:33-44` | abierto |
| H-014 | 2026-08-19 | DocTest F1 | traz-comp-dnato / empresas | deuda | 🟡 | El alta de empresa del superusuario **no valida duplicados** (el control de razón social está comentado) y **no crea** el establecimiento, el depósito ni los usuarios iniciales, a diferencia del alta por registración. Dos caminos para lo mismo, con resultados distintos. **Confirmado como bug por el PM (2026-08-24): los dos altas tienen que validar lo mismo y dejar la empresa igual de configurada** | `Empresa.php:46-105` (control comentado en 118-121) vs `Register::guardarEmpresa()` | en issue #458 |
| H-015 | 2026-08-24 | DocTest F1 | traz-comp-dnato + BD / perfiles | mejora | 🟢 | **Los perfiles de DNATO no se llaman igual en los datos que en el negocio**: `seg.roles` guarda `Admin` y `Author` (herencia de la plantilla original del sistema, junto con `Editor` y `Subscriber` que no se usan), mientras que funcionalmente son *Administrador* y *Usuario*. El usuario ve "Author" en la columna Nivel de Usuario sin que eso signifique nada para él. **Decisión del PM (2026-08-24): se deja como está por ahora**; el catálogo y las ayudas usan el lenguaje de negocio (`Administrador`, `Usuario`) y el renombre de los datos queda como mejora futura | `seg.roles`, `Userlevel.php`, `changeleveluser.php` (campo Perfil) | abierto — futura mejora |
| H-016 | 2026-08-24 | DocTest F1 | traz-comp-dnato / cuenta | bug | 🔴 | **Cualquier usuario puede recuperar la contraseña, aunque nunca haya validado su correo.** La pantalla solo exige que la cuenta esté `approved`, y tanto los cinco usuarios iniciales de cada empresa como los que crea el administrador nacen ya en `approved`. Regla del PM (2026-08-24): la recuperación es solo para quien tenga el correo validado — el administrador que dio de alta la empresa | `Main.php:1505-1512`, `Register.php:1042`, `User_model.php:957` | en issue #455 |
| H-017 | 2026-08-24 | DocTest F1 | traz-comp-dnato / cuenta | bug | 🟢 | **Corregido el 2026-08-25 al verificarlo en la app**: la pantalla **sí** protege el correo (`views/changeuser.php` lo declara `readOnly`), así que por la interfaz no se puede cambiar. Lo que queda es que **el servidor confía en ese readonly**: `updateuser()` guarda el correo que llegue en el POST, y como es la identidad del usuario en Bonita y en `users_business`, un pedido armado a mano desalinea la cuenta. Defensa en profundidad faltante, no un agujero alcanzable desde la pantalla | `views/changeuser.php` (readonly) vs `Main.php:611-672` | en issue #456 |
| H-018 | 2026-08-24 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🟢 | **Corregido el 2026-08-25 al verificarlo con un test**: la pantalla **no** ofrece al propio administrador, así que por la interfaz no puede inhabilitarse. Queda que `banuser()` no compara el correo del POST contra el de la sesión — misma defensa en profundidad faltante que #456 y #460 | `views/banuser.php:8-19` vs `Main.php:500-554` | en issue #459 |
| H-019 | 2026-08-24 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🟢 | **Corregido el 2026-08-25 al verificarlo con un test**: el desplegable **sí** ofrece solo las empresas del administrador (`views/adduser.php:77` usa `emp_connect`, no `obtenerBusines()`). Lo que queda es que el servidor no revalida la empresa que llega en el POST, y que `dd_business` se calcula y viaja a la vista sin que nadie la use | `views/adduser.php:77` vs `Main.php:259` | en issue #460 |
| H-020 | 2026-08-24 | DocTest F1 | traz-comp-dnato / menúes | bug | 🟢 | **Corregido el 2026-08-25 al verificarlo con un test**: el desplegable **sí** ofrece solo las empresas del administrador (`views/menu/menurolesList.php:160` recorre `emp_connect`). Queda que `addMenuRoles()` no revalida el grupo que llega en el POST — misma defensa en profundidad faltante que #456, #459 y #460 | `views/menu/menurolesList.php:160` vs `Menu.php:98-157` | en issue #461 |
| H-021 | 2026-08-24 | DocTest F1 | traz-comp-dnato / registración | mejora | 🟢 | No hay depuración de cuentas registradas que nunca confirmaron el correo: quedan en Dnato sin activar y ocupan la dirección para siempre | `Main::procesarRegistro()` | abierto — mejora futura |
| H-022 | 2026-08-24 | DocTest F1 | traz-comp-dnato / registración | mejora | 🟢 | Las respuestas del formulario de información adicional (cómo se enteró, rubro, tamaño, problemas) se guardan y **no se usan para nada**. Idea del PM: una tool MCP para el superusuario que explote esos datos | `Register::guardarFormularioRegistro()` | abierto — mejora futura |
| H-023 | 2026-08-24 | DocTest F1 | traz-comp-dnato / empresas | mejora | 🟡 | **No hay dónde corregir los datos de la empresa después del alta.** El PM decidió no validar el identificador tributario, lo que hace imprescindible poder editarlo si se cargó mal — y hoy no existe esa pantalla | `Empresa.php` (solo listar, agregar y borrar) | abierto |
| H-024 | 2026-08-24 | DocTest F1 | traz-comp-dnato / empresas | mejora | 🟢 | Los mensajes de error del alta piden "contactá soporte" sin decir cómo: deberían mostrar `soporte@trazalog.com` | `Register.php` (mensajes de `guardarEmpresa`) | abierto |
| H-025 | 2026-08-24 | DocTest F1 | traz-comp-dnato / usuarios | mejora | 🟢 | La etiqueta "Rol" del alta de usuario es en realidad el **perfil de Dnato** (Admin/Author), no un rol de trabajo: se confunde con los roles de Tools | `application/views/adduser.php` | abierto |
| H-026 | 2026-08-24 | DocTest F1 | traz-comp-dnato / configuración | mejora | 🟡 | La pantalla de Configuración del sitio (título, zona horaria, reCAPTCHA, tema) **queda fuera de alcance y probablemente haya que ocultarla**: es global del sistema y la puede tocar cualquier administrador de cualquier empresa | `Main::settings()` | abierto — caso DNATO-UC-021 marcado obsoleto |
| H-027 | 2026-08-24 | DocTest F1 | traz-comp-dnato / menúes | mejora | 🟢 | El ABM de opciones de menú es muy rústico (sin validaciones declaradas, campos técnicos expuestos). Candidato a rehacerse, sin prioridad | `Menu.php`, `views/menu/menuesList.php` | abierto — mejora futura |
| H-028 | 2026-08-24 | DocTest F1 | traz-comp-dnato / empresas | bug | 🔴 | **"Eliminar empresa" no borra la empresa: borra un usuario.** `Empresa::borrarEmpresa($empr_id)` es una copia del borrado de usuarios — busca un usuario con el id de la empresa, usa `$busines` sin definir y termina en `deleteUser($empr_id)`, un `DELETE` físico sobre `seg.users` por id. Si algún usuario tiene ese id, se borra el usuario equivocado y la empresa sigue existiendo. El modelo ya tiene `Empresas::eliminarEmpresa()`, que es la correcta, y la pantalla no la usa | `Empresa.php:132-176` | en issue #462 |
| H-029 | 2026-08-24 | DocTest F1 | traz-comp-dnato / carga masiva | bug | 🟡 | **La carga masiva no verifica el perfil**: el controlador solo exige sesión iniciada (`if (!$this->session->userdata('email'))`). El menú la muestra solo a administradores, pero cualquier usuario autenticado llega por dirección directa y puede cargar datos | `Bulkload.php:18-48` | abierto |
| H-030 | 2026-08-24 | DocTest F1 | traz-comp-dnato / carga masiva | bug | 🟡 | **La carga masiva de Equipos falla**: el procedimiento no funciona contra MariaDB, que es donde viven los equipos (Asset Planner). Las otras entidades —Artículos, Herramientas y Stock de Artículos— sí cargan. **El PM lo quiere resuelto en el corto plazo** | `Bulkload::procesarCarga()` + procedimientos `sta.sp_carga_masiva_*` | abierto — corto plazo |
| H-031 | 2026-08-24 | DocTest F1 | traz-comp-dnato / cuenta | bug | 🟡 | **La pantalla "Perfil" queda en blanco.** `views/profile.php:8` llama a `image()`, pero el helper define `imageAdmin()`: es un *call to undefined function*, error fatal, y la respuesta llega vacía (200 con `<html><head></head><body></body></html>`). El usuario ve una página en blanco sin ningún mensaje. **Lo encontró el primer test automatizado de la suite**, no una lectura de código | `views/profile.php:8` vs `helpers/image_helper.php:6` | en issue #463 |
| H-032 | 2026-08-25 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🔴 | **El alta de usuarios termina en un callejón sin salida**: la pantalla avisa que hay que asignarle roles desde la Lista de Usuarios, pero el usuario recién creado **no aparece ahí**, porque `getListUserDataForGroups()` hace INNER JOIN contra `memberships_users` y él todavía no tiene ninguna. Solo se lo ve después de asignarle un rol, que es lo que no se puede hacer. Verificado: sí figura en "Habilitar/Deshabilitar Usuario", que usa otra consulta | `User_model.php:730-732` | en issue #464 |
| H-033 | 2026-08-25 | DocTest F1 | traz-comp-dnato / cuenta | bug | 🟡 | **Restablecer la contraseña termina en una página en blanco.** La contraseña sí cambia, pero `reset_password()` redirige a `checkLoginUser()`, que está escrito para alguien que ya inició sesión: usa `$data['email']`, `first_name` y `last_name` —que en este flujo no existen— para mandar el correo de "nuevo inicio de sesión". El usuario nunca ve "Tu contraseña se actualizó correctamente" ni vuelve al ingreso | `Main.php:1587-1629` → `Main.php:64-115` | en issue #466 |
| H-034 | 2026-08-25 | DocTest F1 | traz-comp-dnato / cuenta | bug | 🔴 | **Recuperar la contraseña no cambia la contraseña.** `updatePassword()` es la única consulta del modelo que escribe en `users` **sin el esquema `seg.`**, así que el UPDATE no toca la fila del usuario. Verificado con el ciclo completo: después de "cambiarla", la nueva da credenciales incorrectas y la vieja sigue funcionando. Es el único camino de recuperación que tiene un usuario, y no lleva a ningún lado — agravado por #466, que además le oculta cualquier señal de error | `User_model.php:379` | en issue #467 |
| H-035 | 2026-08-25 | DocTest F1 | traz-comp-dnato / usuarios | bug | 🔴 | **Editar un usuario no funciona: la pantalla no existe.** `Main::edituser($id)` declara seis reglas de validación, arma el título y termina **sin cargar ninguna vista**, y en `views/` no hay ningún `edituser`. La dirección devuelve una página vacía de 39 bytes. Sin esto, un usuario mal cargado solo se puede "arreglar" dándolo de baja —que además borra físicamente (#451)— y creándolo de nuevo | `Main.php:742-771`, `application/views/` | en issue #468 |

---

## Origen de los hallazgos

| Origen | Qué es |
|---|---|
| `DocTest F1` | Relevamiento del catálogo funcional de DNATO (issue #438) |
| `DocTest F2..F5` | Fases siguientes de DocTest |
| `<tarea>` | Cualquier otra tarea del `STATE.md` que haya encontrado el hallazgo |
